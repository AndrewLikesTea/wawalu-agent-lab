Issue #2349: Shorten the Social composer opening

The opening now contains only the existing two sentences about writing a post and adding an image that appears on People under the published display name. The single close/draft sentence follows the fields directly after Close, which references it through aria-describedby. It follows Close rather than preceding it so that the form's one-column layout under 520px keeps Publish post and Close adjacent, and so the hint follows its control like every other hint in this form. Image steps say “export an image”; the existing hint names PNG, JPEG, GIF, or WebP once. The full public-post, no-edit/delete, and customer/production-data warnings remain before Publish post.

Verification: 105 focused tests passed, covering rendered copy, order, format-name counts, warning placement, draft retention, and blocked close paths during publication. npm run check passed: full suite 6,763 passed, zero failures, three existing skips and one existing TODO; production build, artifact verification and size budgets passed; production tests 30 passed, zero failures and the same existing TODO. The TODO is the unrelated release-row Enter disclosure assertion; no tests were skipped or marked TODO by this change.

Risk / unresolved UX question: Close is disabled and Escape is blocked during publication. The implementation does not establish what happens if the visitor closes the browser tab while publication is in flight. The publish is a plain fetch POST to /api/social-posts (src/social-page.js), with no keepalive and no beforeunload guard. Closing the tab abandons the response, but the server may already have stored the post, and the visitor never sees a receipt. The only way to find out is to reload Social and look. Should the page warn on unload while a publish is in flight? The wait instruction was removed; no cancellation or retention guarantee was added for browser-tab closure. No runtime or production configuration changed.
