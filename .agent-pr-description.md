Closes #2408.

The shared post page (`/post.html`) is the one surface a stranger meets cold, from a link pasted into a chat window. The post region already reads first — that landed earlier — but the note under it was still Social's whole feed note: three sentences and forty words about display names, sitting directly beneath a sentence that had just named the invented posts.

Social ships that block because Social is a feed of many posts by many names, and a reader scrolling it is deciding what to trust. This page holds one post. The one fact its reader needs is who picked the name in the byline.

## The sentence

Old, on `/post.html` only:

> Display names on the posts already on Social are invented. On any other post, whoever published it chose the name. Nobody owns or verifies a display name, and anyone can publish under any name.

New:

> Whoever published this post chose the display name on it; nobody owns or verifies a display name on Social.

40 words to 17. Social and People keep the three-sentence note unchanged — they are separate queued issues and are not touched here.

- It names who chose *this* post's display name, which is the question a permalink reader has.
- It keeps the part that protects them: nobody verifies a name.
- It drops the restatement of the invented posts. The paragraph directly above it already says that, once: "The posts already on Social are invented to demonstrate Shiplog and use no customer or production data; a post a visitor publishes is real." That sentence is unchanged, and still the page's only "no customer or production data" claim.
- The opening sentence is unchanged: "This page is for one post from Social, Shiplog's shared feed of short posts about shipped work."

## Files

- `src/post.html`: the standing `.hint` in the page frame, outside `#post-detail`, so no state paints it and no script rewrites it. Same class, same position — after the post, before the reporting paragraphs and the routes out.

No script change: `src/post-page.js` and `src/post-detail.js` never wrote this line, and `post-detail.js` already declines to paint a display-name explanation of its own.

## Tests

- `tests/post-page-flow.test.js`: `IDENTITY` is now this page's own sentence; `FEED_NOTE` holds Social's three, read out of `social.html` and `profile.html` so a silent migration back fails here. `assertIdentityStands` now pins the **rendered** order — the combined query includes `#post-detail`, so heading → post region → the sentence → the Social link is asserted on the DOM a visitor actually gets, in all five states (cold markup, loading, loaded, not-found, error), not on authored markup order.
- New test: "the permalink opens with the post in every state, not with its caveats". It walks `#main-content` in rendered document order and asserts that everything above the post region is exactly the eyebrow and the page's own sentence, that none of it matches `/invented|display name|customer or production data|Report post|reporting/i`, and that it is at most 25 words — the measure the issue was written about.
- `tests/post-detail.test.js`: the "no state paints its own display-name explanation" check now refuses both wordings.

## Results

- `tests/post-page-flow.test.js`, `tests/post-detail.test.js`, `tests/post-permalink-states.test.js`, `tests/post-onward-destinations.test.js`, `tests/social.test.js`, `tests/site-nav.test.js`, `tests/people-landing.test.js`, `tests/site-footer.test.js`, `tests/page-skip-link.test.js`, `tests/post-follow-up-copy.test.js`, `tests/post-page-directory-disclosure.test.js`, `tests/follow-up-privacy.test.js`, `tests/build.test.js`: all pass.
- Full `npm test`: RESULT_PLACEHOLDER
- `npm run build` then `npm run verify:build`: pass, every tracked artifact inside its budget. (The size budget gates the evolution pages only; `post.html` is not gated, and this change is a net cut to it anyway.)

## Observed, deferred

- The browser title on this page reads "Social post · Social · Shiplog" before a post loads — "Social" twice, and a nav name where the surface name should be. Not touched here; it belongs with the page's title/heading pair rather than with a caveat edit.
- Five pages phrase the same wait five ways ("The post is loading.", "Loading releases", and three more). Worth one pass across all of them, not a one-page fix.

🤖 Generated with [Claude Code](https://claude.com/claude-code)
