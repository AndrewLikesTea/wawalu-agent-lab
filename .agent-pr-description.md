Closes #2484.

People's "To add yours" helper closed on a pointer to "Social's publishing terms". Social's composer notice has no heading, so that name pointed at nothing a visitor could find. People now states the conditions itself. The note is authored in `src/profile.html`. `src/profile.js` only removes and restores the paragraph while the first fetch runs and never rewrites its text, so the authored string is what visitors see.

**People, before**

> A published post is public and cannot be edited or deleted; [Social’s publishing terms] state the rest.

**People, after**

> A published post is public and cannot be edited or deleted, and anyone can select Report post on it. Do not include customer or production data.

The "Social’s publishing terms" link (`#profile-terms-route`) is gone with the pointer. It went to the same `#post-form` href as "Publish a post on Social", so no destination is lost. The only change to the keyboard sequence is one fewer tab stop. No headings, links or controls were added.

**Social: unchanged.** Social's composer notice (`#post-consequence`) already states all four conditions and nothing extra: "Anyone who visits Shiplog can read your post, its image, and the display name you publish it with. You cannot edit or delete your own post after you publish it, so post nothing you would not put on a public page. Anyone can select Report post on a published post. Do not include customer or production data." The "post nothing you would not put on a public page" clause is advice that follows from the post being public. It is not a separate condition.

| Condition | People (`.feed-create`) | Social (`#post-consequence`) |
|---|---|---|
| Public | "A published post is public" | "Anyone who visits Shiplog can read your post…" |
| Can't be edited or deleted | "cannot be edited or deleted" | "You cannot edit or delete your own post after you publish it" |
| Report post | "anyone can select Report post on it" | "Anyone can select Report post on a published post." |
| No customer/production data | "Do not include customer or production data." | "Do not include customer or production data." (same bytes) |

People's count of "no customer or production data" is still exactly 1 (only the provenance sentence), because the new sentence says "Do not include…".

**"publishing terms"** no longer appears in any reviewed page: profile, social, post, index, releases or coach.

**Display-name phrase.** For a writer's own post, both pages use Social's phrase: "the display name you publish it with". People's helper already said "A published post with an image appears on People, under the display name you publish it with." Social's composer says the same thing in its hint and its notice. I left People's third-person "published under" in the tagline, the picker hint and "See every post published under X on Social" as it is. Those lines describe other people's posts, and "you publish it with" can't be made to fit them grammatically. Social's visible copy doesn't use "published under" at all. If reviewers want a single phrase for the third-person case too, that needs its own copy decision, and it touches the shared `socialAllPostsLabel` in `src/social-links.js` and pins in about six test files.

**Tests updated**
- `tests/people-landing.test.js`: adds a `PUBLISH_CONDITIONS` table pairing People's and Social's wording for each condition. It asserts on the served and on the painted helper (after a non-skeleton tile wait) that all four are present, that there is no "publishing terms" on People or Social, and that the display-name phrase is used. The route-to-Social count drops from 4 to 3.
- `tests/image-creation-path.test.js`: the new closing sentence, a single Social link in the helper, a new test "no reviewed page points at Social's publishing terms", and removed pins on the terms link.
- `tests/post-report-flow.test.js`: People now states both halves of the edit/report distinction in its own words, including the "Report post" label.

**Verification:** the affected suites (profile, people-*, social*, post-*, feed-*, follow-up-*, site-nav, build and others that load profile.html) all pass: 655 tests, 0 fail. `npm run build` and `npm run verify:build` both pass. I did not run the full `npm test`.

🤖 Generated with [Claude Code](https://claude.com/claude-code)
