Social and People can contain real visitor posts, so their page eyebrows now read “Social · public posts” and “People · public image posts” instead of “Social · demo” and “People · demo.” Only the tests pinning the old Social label changed.

The shared Post eyebrow remains “Social.” Page introductions, invented-versus-real post warnings, publishing consequence text, and navigation remain unchanged.

Verification: DOM checks of source and production HTML confirm the exact labels above their intended page headings and no “demo” in the shared Post eyebrow. All 141 focused tests passed. `npm run check` passed, including the full test suite, production build, artifact verification, size budgets, and production journey tests. The standalone `npm run build` also passed. The suites report an existing TODO for Enter-key activation of a release row.

Risk is low: this changes two static labels, with no behavior changes. The navigation’s “Demos” label has the same contradiction but requires a separate product decision. That decision and the ticket’s smaller copy issues remain outside this change.
