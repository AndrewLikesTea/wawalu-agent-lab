// Scratch file — delete before merge. It held the one-off script that seeded
// src/model-overspend-finding-fixture.json. The script is redundant now:
// tests/model-overspend-finding.test.js rebuilds the fixture's `finding` from
// the fixture's own `input` and fails on any drift. This worktree denies `rm`
// and `mv`, so the file could not be removed here.
