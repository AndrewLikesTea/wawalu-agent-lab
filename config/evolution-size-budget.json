{
  "$comment": "Size budget for what /evolution.html sends a reader before they interact with anything. Enforced by scripts/check-size-budget.mjs against dist/, wired into `npm run verify:build` and therefore into `npm run check`. Every budgetBytes below is a MEASUREMENT of the post-collapse page as built on 2026-07-30 from this branch, plus the stated headroom — not a target anyone hopes to reach.",
  "headroom": "2% over the measured baseline, rounded up to the next whole KiB",
  "$headroomRationale": "Small on purpose. The value of this check is that it notices an unplanned addition, so wide headroom would let a module-sized regression land silently and no headroom would fail on a comment. 2% is roughly a paragraph of prose on the document and roughly one small module on the entry graph, which is the granularity worth arguing about in review.",
  "$deferralNote": "Honest accounting of the deferral that landed with this file, because the numbers did not come out the way the change was scoped to. Moving the comparable-peer method prose out of evolution.html took 2,343 bytes out of the document; the fallback paragraph and the comment explaining it put 2,125 back, so the document is 177,349 bytes against 177,567 before — a net 218 bytes. The loader that makes the deferral work, src/finops-deferred-detail.js, is a hard import of the page entry, so the entry graph is about 8 KiB LARGER than before. The initial payload therefore grew, not shrank. The reason is that this page has no evidence MODULE that is disclosure-only: every figure on it, including the seven headline disclosures, is composed eagerly by modules the headline itself depends on (ranking-reproducibility.js, for instance, also supplies the headline's laggard name and provenance), so none of them can leave the initial payload without moving a figure the answer rests on. What is deferrable here is authored prose, and one panel of prose does not pay for its own loader. The mechanism is in place and the marginal cost of the next panel is only the prose it removes; these baselines record where the page actually is today rather than where a second panel would put it.",
  "artifacts": [
    {
      "id": "first-screen",
      "label": "first screen (answer block readable)",
      "kind": "sum",
      "parts": [
        "evolution-html",
        "evolution-css",
        "styles-css",
        "journey-consolidated-css",
        "evolution-entry-graph"
      ],
      "budgetBytes": 5273600,
      "baseline": "RAISED ON PURPOSE for #1207 by 21 KiB, being its ONE part and nothing else: the entry graph, itemised in its own baseline below. NEITHER STYLESHEET WAS RAISED and THE DOCUMENT DID NOT MOVE — the file control is authored on /executive-briefing.html, which is not this page, and the only thing that reaches /evolution.html is the shared envelope module the link codec on this page now consumes instead of defining its payload inline. It adds no rule to either sheet and no focusable node to this page, so the first screen's tab order is byte-for-byte the one it had. The bytes buy the property #1206 could not have on its own: the link and the downloaded brief file are ONE envelope, defined and validated in src/finops-brief-envelope.js, so a disclosure the link carries — the confidence grade and its meaning, the provenance, the Limits statements — cannot go missing from the file, and a build that emits a brief its own reader would refuse fails on the sender's side. Measured 5,149.7 KiB in dist/ on 2026-08-06, rounded up to the next whole KiB. Previous baseline note follows. RAISED ON PURPOSE for #1210 by 14 KiB, being its two parts and nothing else, itemised in their own baselines below: about 1 KiB of document and about 13 KiB of entry graph. NEITHER STYLESHEET WAS RAISED and neither moved — the parity verdict is one more .prompt-coaching-copy-status paragraph inside the share control that already ships, so it adds no rule to either sheet, and it adds no focusable node, so the first screen's tab order is byte-for-byte the one it had. The bytes buy the one thing #1206 could not tell a sender: whether the link they are about to paste reproduces their headline figure, their destination and their confidence grade on the recipient's side. The check runs the shipped codec and the recipient's own briefing builder rather than a description of them, which is what most of the 13 KiB is and why it is not cheaper: a second opinion about the same figures would be a parity check that agrees with itself. It is on the first screen because it is a verdict about a control that is on the first screen, and a verdict that arrives after the press is one the lead has already sent past. Measured 5,128.7 KiB in dist/ on 2026-08-06, rounded up to the next whole KiB. Previous baseline note follows. RAISED ON PURPOSE for #1206 by 37 KiB, being its two parts and nothing else, itemised in their own baselines below: about 2.5 KiB of document and about 34 KiB of entry graph. NEITHER STYLESHEET WAS RAISED and neither moved — the shareable-link control reuses .prompt-coaching-copy, its lead, its status and its fallback, the design system secondary button and a prompt-coaching-field box, exactly as the copy control beside it does, and adds no rule to either sheet. The bytes buy the one thing a lead could not do with this page's answer: send it. A plain URL opened in a colleague's browser resolves to the published synthetic sample, so a shared analysis silently became an invented company's figures under the sender's sentence about ours; the link now carries the sender's own retained periods in its fragment. Measured 5,114.6 KiB in dist/ on 2026-08-06, rounded up to the next whole KiB. Previous baseline note follows. RAISED ON PURPOSE for #1196 by 8 KiB, being its ONE part and nothing else: the entry graph, itemised in its own baseline below. NEITHER STYLESHEET WAS RAISED AND NEITHER WAS THE DOCUMENT — the graded floor's markup fits inside the headroom evolution.html was already carrying, and its disclosure is mounted rather than authored. Measured 5,077.6 KiB in dist/ on 2026-08-06, rounded up to the next whole KiB. Previous baseline note follows. RAISED ON PURPOSE for #1195 by 21 KiB, being its two parts and nothing else, itemised in their own baselines below: about 2 KiB of document and about 18 KiB of entry graph. NEITHER STYLESHEET WAS RAISED. The bytes buy a lead the answer as pasteable text with the qualifying label carried in it, so a figure that leaves this page cannot leave the sentence saying whose data it is behind. Measured in dist/ on 2026-08-06, rounded up to the next whole KiB. Previous baseline note follows. RAISED ON PURPOSE for #1186 by 7 KiB, being its two parts and nothing else: about 1 KiB of document and about 15 KiB of entry graph, itemised in their own baselines below, against 9 KiB of headroom this sum was already carrying. NEITHER STYLESHEET WAS RAISED. Measured 5,048.8 KiB in dist/ on 2026-08-06, rounded up to the next whole KiB. Previous baseline note follows. RAISED ON PURPOSE for #1185 by 16 KiB, being its one part and nothing else: the three how-we-know disclosures in the document. NEITHER STYLESHEET WAS RAISED and neither moved — the pattern reuses .figure-source, .figure-source-summary, .figure-source-state, .figure-source-shape and .figure-source-detail whole and adds no rule to either sheet, and the entry graph is unchanged. The bytes buy a CTO the finding before its qualification: the 233-character hedge between the recoverable figure and the move it implies is now 69 characters plus a one-word Illustrative marker, and the provenance, basis and limits it used to shout are one press away in one learnable disclosure, verbatim, with the internal identifiers a skeptic needs kept inside it rather than deleted. They are on the first screen because a proof a reader has to leave the answer to reach is a proof they will quote past. Previous baseline note follows. Measured 5,025.7 KiB in dist/ on 2026-08-05, rounded up to the next whole KiB. RAISED ON PURPOSE for #1183 by 3 KiB, being its two parts and nothing else: the answer region's own document bytes and the manifest entry that classifies it. The page led with four sections that each posed its primary question and none that answered it; a reader who could not act until they had read all four was paying far more than 3 KiB for the ambiguity. Was: 5,143,552 after #1169. PRIOR NOTE: RAISED ON PURPOSE for #1169 by 18 KiB, which is TWO of its parts and nothing else: about 2.5 KiB of document and about 14.6 KiB of entry graph, itemised in their own baselines below. NEITHER STYLESHEET WAS RAISED — the one-question on-ramp reuses .provider-readiness, .provider-readiness-title, .provider-readiness-item, .provider-readiness-where, .provider-readiness-key, .provider-readiness-columns, .provider-readiness-download, .import-slot-label, .import-slot-state, .import-slot-unlocks, .local-import-field, .local-export-primary and .eyebrow whole, and no CSS rule was added or changed in either sheet. The bytes buy the first decision a visitor makes on this panel: one provider choice, answered in place with that adapter names report, the files that start it, and exactly one next action, with the column list, the second file the figure needs and the other providers folded behind disclosures. It is on the first screen because it is the door to the import — a chooser a reader has to open something to reach answers nothing. Previous baseline note follows. Measured 5,004.6 KiB in dist/ on 2026-08-05, rounded up to the next whole KiB. RAISED ON PURPOSE for #1168 by 1 KiB, which is TWO of its parts and nothing else: about 0.4 KiB of document and about 0.6 KiB of entry graph, itemised below. NEITHER STYLESHEET WAS RAISED — the downgraded spend-only result reuses .own-data-preflight-sources whole and adds no CSS rule. The bytes buy a billing-only export a result instead of a refusal: the computed figures it does support, and one named withheld figure per figure it does not, each carrying the structural field that is missing and the pinned recipe that supplies it. It is on the first screen because it is painted into the evidence-preflight region that already ships there, and a withheld figure a reader has to open something to find is one they will quote past. Previous baseline note follows. Measured 5,003.1 KiB in dist/ on 2026-08-05, rounded up to the next whole KiB. RAISED ON PURPOSE for #1166 by 10 KiB, which is TWO of its parts and nothing else: about 1 KiB of document and about 8 KiB of entry graph, itemised in their own baselines below. NEITHER STYLESHEET WAS RAISED — the recipe rows reuse .import-slot, .import-slot-label, .import-slot-state, .import-slot-unlocks, .provider-readiness, .provider-readiness-title, .provider-readiness-lead, .provider-readiness-key and .provider-readiness-columns whole, and no CSS rule was added or changed. The bytes buy the decision a platform lead makes BEFORE they open a console: for each pinned intake adapter, the one report to pull, the question it answers, and whether that file supplies spend only — so a lead who needs the headline figure learns it needs a query sample too before the errand rather than after it. It is on the first screen because the list is painted at boot beside the picker that asks for the file, and a recipe a reader has to open something to reach is a recipe they never see. Previous baseline note follows. Measured 4,993.8 KiB in dist/ on 2026-08-05, rounded up to the next whole KiB. RAISED ON PURPOSE for #1141 by 16 KiB, which is TWO of its parts and nothing else: about 0.8 KiB of document and about 15.7 KiB of entry graph, itemised in their own baselines below. NEITHER STYLESHEET WAS RAISED — the disclosure reuses .completeness-detail and .answer-figure-basis whole, and the snippet block is the shipped monospace preview rule with one selector appended to it (23 bytes, inside evolution.css's existing headroom), so no rule was added to either sheet and evolution.css still measures under its unchanged budget. The bytes buy the question the downloaded policy raises and could not answer: what these ranked rules turn into on the shape a request router is actually configured in, worked through on the reader's OWN policy rather than an example, with the rules that cannot be expressed named and counted rather than dropped. It is on the first screen because it is one collapsed disclosure inside the routing-slate section the reader is already reading, and because the module that translates it is imported by the view that paints that section on the first paint. Previous baseline note follows. Measured 4,977.4 KiB in dist/ on 2026-08-05, rounded up to the next whole KiB. RAISED ON PURPOSE for #1138 by 13 KiB, which is TWO of its parts and nothing else: about 1.3 KiB of document and about 12 KiB of entry graph, itemised in their own baselines below. NEITHER STYLESHEET WAS RAISED — the download control reuses .secondary-button and the shipped .visually-hidden utility and adds no CSS rule. The bytes buy the ranked rules an existence off this screen: a versioned JSON policy document, composed in the reader's own browser from the slate already painted, stating in its own words that it is a proposal derived from their export and must be reviewed before anything reaches a live gateway. Previous baseline note follows. Measured 4,964.3 KiB in dist/ on 2026-08-05, rounded up to the next whole KiB. RAISED ON PURPOSE for #1139 by 25 KiB, which is TWO of its parts and nothing else: about 2 KiB of document and about 23 KiB of entry graph, itemised in their own baselines below. NEITHER STYLESHEET WAS RAISED. The bytes buy the answer the routing slate could not give: whether the rules shipped last period actually returned what they said they would, with the dollar threshold that produced each verdict on the record rather than recomputed at render time. Previous baseline note follows. Measured 4,939.9 KiB (5,058,444 bytes) in dist/ on 2026-08-05, rounded up to the next whole KiB. RAISED ON PURPOSE for #1140 by 12 KiB, which is ONE of its parts and nothing else: the entry graph, itemised in its own baseline below. THE DOCUMENT DID NOT MOVE past its budget (one status paragraph in, one paragraph of comment out) and NEITHER STYLESHEET WAS RAISED — the lifecycle chips reuse .import-chip whole and the hierarchy reuses the .answer-figure trio, .action-list and .completeness-detail; the only CSS change is overflow-wrap:anywhere added to the existing .action-list rule. Previous baseline note follows. Measured 4,927.4 KiB (5,046,131 bytes) in dist/ on 2026-08-05, rounded up to the next whole KiB. RAISED ON PURPOSE for #1137 by 15 KiB, which is the sum of its two parts that grew and nothing else: about 0.7 KiB of document and about 14 KiB of entry graph, itemised in their own baselines below. NEITHER STYLESHEET WAS RAISED — the routing slate reuses .finops-panel, .answer-question, the .answer-figure trio, .action-list and .completeness-detail, and adds no CSS rule. The bytes buy a reader the thing the recoverable-spend diagnosis never gave them: the routing changes to ship this month, ranked by what each is worth, under one total and one next action. Previous baseline note follows. Measured 4,912.4 KiB (5,030,297 bytes) in dist/ on 2026-08-05, rounded up to the next whole KiB. RAISED ON PURPOSE for #1126 by 6 KiB, which is ONE of its parts and nothing else: the entry graph, itemised in its own baseline below. No markup and no stylesheet moved. The bytes are the query-sample contract validator, which the bundled example now parses its scored sample through so the trust panel reports a coverage figure and a grade the rubric actually produced instead of reporting that it scored none of the $154,500 in scope. Previous baseline note follows. Measured 4,906.1 KiB (5,023,772 bytes) in dist/ on 2026-08-05, rounded up to the next whole KiB and no percentage on top, so the next unplanned byte still fails this check. RAISED ON PURPOSE for #1112 by 3 KiB, which is ONE of its parts and nothing else: the entry graph, itemised in its own baseline below. The document did not move — this change authors no markup — and neither stylesheet moved, because the reorder is a runtime DOM move rather than a rule. The bytes buy a first-time visitor the thing the homepage sent them here for: with no period on file, the bundled worked decision is the first block under the page's name and the 'No track record on file' invitation reads directly beneath it, instead of an empty state about a file nobody asked them to bring leading the page. Previous baseline note follows. Measured 4,903.2 KiB in dist/ on 2026-08-05, rounded up to the next whole KiB. RAISED ON PURPOSE for #1104 by 21 KiB, which is the sum of its two parts that grew and nothing else: about 2.8 KiB of document and about 16.6 KiB of entry graph, itemised in their own baselines below. NEITHER STYLESHEET WAS RAISED — the ladder is two rules inside evolution.css's existing headroom, because its rungs are the .brief-provenance chip this page already ships and no new token, glyph or chip class was introduced. The bytes buy a reader the qualifier every figure on this screen was already carrying silently: which of Declared, Estimated and Verified this brief is on, what that rung supports and what it does not, and the one step that moves it up — all three rungs visible at once, with the current one marked by its place in words and a filled mark before any fill, and nothing in the block focusable. Previous baseline note follows. Measured 4,882.6 KiB in dist/ on 2026-08-05, rounded up to the next whole KiB. RAISED ON PURPOSE for #1105 by 21 KiB, which is the sum of its two parts that grew and nothing else: about 1.8 KiB of document and about 17.8 KiB of entry graph, itemised in their own baselines below. Neither stylesheet moved — the roster import reuses the field and detail classes the intake already ships, so no rule was added to either. The bytes buy a reader a way to stop guessing the denominator under every per-engineer figure on this screen: an engineer headcount counted in this tab out of a two-column HRIS roster, with no request, no credential and no stored copy, and with any file carrying a name, an email, a salary or any other unlisted column refused outright rather than quietly stripped. Previous baseline note follows. Measured 4,861.9 KiB in dist/ on 2026-08-05, rounded up to the next whole KiB. RAISED ON PURPOSE for #1106 by 13 KiB, which is ONE of its five parts and nothing else: the entry graph, itemised in its own baseline below. The document did not move — this change authors no markup, and the one line it adds to the track-record header is painted at runtime from what this browser itself holds. Neither stylesheet moved: the delta line reuses the basis paragraph class the header already ships. The bytes buy the record an estimate leaves behind: a visitor who answers the five facts has their declared position kept as a period entry, and when a real import for that month arrives the header states, in one sentence, how far the estimate was out. Previous baseline note follows. Measured 4,848.1 KiB in dist/ on 2026-08-05, rounded up to the next whole KiB. RAISED ON PURPOSE for #1103 by 23 KiB, which is the sum of its two parts that grew and nothing else: 4,854 bytes of document and about 17.9 KiB of entry graph for the five-fact intake, itemised in their own baselines below. The bytes buy the first thing on this screen a visitor can answer: five facts they already know, no file, no upload, no account, and a headline, a modelled recoverable amount, and one next action back. Previous baseline note follows. Measured 4,825.3 KiB in dist/ on 2026-08-03, rounded up to the next whole KiB. RAISED ON PURPOSE for #1102 by 41 KiB, which is the sum of its two parts that grew and nothing else: 4,498 bytes of document and about 36.3 KiB of entry graph for the estimated cost-per-successful-task position, itemised in their own baselines below. The bytes buy the one figure on this screen that needs no file: a leader who has declared nothing still meets a cost per successful task, a quartile, and a modelled recoverable range, each marked 'estimated' and each openable to its own arithmetic. Previous baseline note follows. Measured 4,784.6 KiB (4,899,375 bytes) in dist/ on 2026-08-03, and rounded up to the next whole KiB — no percentage headroom on top, matching what #1092 did, so the next unplanned byte still fails this check. RAISED ON PURPOSE for #1091 by 79.1 KiB, which is the sum of its two parts that grew and nothing else: 2,379 bytes of document and 76.8 KiB of entry graph for the track-record header, itemised in their own baselines below. Nearly all of the entry-graph number is one decision — the header reuses the shipped scoring module instead of carrying a second opinion about the same commitment — and the case for spending it is made there. PREVIOUSLY 4,705.4 KiB for #1092 by 17.3 KiB, which is the sum of its two parts that grew and nothing else: 2,258 bytes of document and 15.1 KiB of entry graph for the portable track record, itemised in their own baselines above. PREVIOUSLY 4,688.1 KiB for #1089,by EXACTLY what this change adds and no more, with the 8.2 KiB of headroom this line already had left untouched. The bytes are the entry-graph growth itemised on that artifact (one new module at 9,142 bytes plus about 1.9 KiB across two modules the entry already carried) and 151 bytes of evolution.html: one empty paragraph and the comment above it, for the periods-on-file line painted above the answer. The document ships that slot EMPTY — the count, the span and every month name are painted at runtime from what this browser itself holds, so no figure and no reader data is in these bytes. Previous baseline note follows. Measured 4,593.1 KiB in dist/ on 2026-08-03. RAISED ON PURPOSE for #1027, and itemised rather than rolled up: this change adds EXACTLY 24,344 bytes of source across five files (git diff, added minus removed) — about 22.9 KiB of entry graph in src/finops-briefing-export.js, src/finops-briefing-restore.js and src/local-import-flow.js, and 1,450 bytes of evolution.html for the five empty slots the reopened brief's naming is painted into. NO module was added: the naming block is written by the writer that already writes the file and read by the reader that already reads it, and the document ships those slots EMPTY — every unit name, every source column and every correction sentence is painted at runtime from the file the reader chose, so no reader data and no figure is in these bytes. The bytes are on the first screen because that is where the reopen control and the restored region already are; a brief whose unit names arrive a request after its figures is one the reader has already read under the wrong names. The remaining ~85 KiB of the gap to the old 4,685,824 budget is drift that had already landed against the old headroom since 4,593,357 bytes was measured for #1020, recorded as its own line rather than folded into this change's number. The rest is the file's stated 2% headroom, restored over the new measurement. Previous baseline note follows. Measured 4,485.7 KiB (4,593,357 bytes) in dist/ on 2026-08-03. RAISED ON PURPOSE for #1020, and itemised rather than rolled up: this change adds EXACTLY 17,608 bytes to the first screen — 16,128 of entry graph (src/finops-bundled-next-step.js at 11,864 bytes, which derives the bundled example first action, and src/finops-bundled-next-step-view.js at 4,264 bytes, which paints it), 1,480 of src/evolution-page.js (the imports, the memoised derivation, the named data-state branch on both paints, and the circulation call site) and 620 of evolution.html (two replaced placeholder sentences and the comments stating why they exist). The bundled dataset itself, src/evolution-demo-data.json, is a JSON import and is NOT counted in the graph; it was already served to this page. The rest of the gap to the old 4,571,136 budget is drift that had already landed against the old headroom since 4,480,895 was measured for #997. WHY THE BYTES ARE ON THE FIRST SCREEN and not behind a disclosure: both modules answer what a reader with no file imported should do first, in the region that used to answer no journey records have been read yet, so no next step is recommended. A step that arrives after the fixture fetch is a step the reader has already met a dead end instead of, which is the same defect #944 closed one layer up. NO figure, NO department and no reader data is authored in the document: every value is derived from the bundled dataset by the module and painted. Previous baseline note follows. Measured 4,375.9 KiB (4,480,895 bytes) in dist/ on 2026-08-02. RAISED ON PURPOSE for #997, and itemised rather than rolled up: this change adds EXACTLY 15,613 bytes to the first screen — 12,188 of entry graph (the two-slot model and its painter, plus the page wiring and the suppression flag the retention payload now carries), 2,060 of evolution.html (the two slot nodes, the literacy card's reason line, and the comments stating why they exist) and 1,365 of evolution.css. The remaining 75,658 bytes of the gap to the old 4,477,952 budget is drift that had already landed against the old headroom since 4,389,624 was measured for #979: the first screen without this change measures 4,465,282 bytes, still inside the old budget, so this change is what crossed the line and it is 15 KiB of it. The rest is the file's stated 2% headroom, restored over the new measurement. The bytes belong on the first screen because the two slots and the card's reason line answer 'why is there no grade?' at the moment a reader is looking at the absence — a sentence that arrives after paint is one they have already read a dash instead of. NO figure, NO grade and no reader's file name is in these bytes: the slot wording and the withheld sentence are painted from src/finops-import-slots.js. Previous baseline note follows. Measured 4,286.7 KiB (4,389,624 bytes) in dist/ on 2026-08-02. RAISED ON PURPOSE for #979, and itemised rather than rolled up: this change adds EXACTLY 14,691 bytes to the first screen — 14,266 of entry graph (the composer that ranks the reader's own groups, plus the imported state in the first-run view and its two call sites) and 425 of evolution.html (four ids and the comment stating why they exist; NO figure, NO row, and no reader's data ships in the document). The remaining 79,002 bytes of the gap to the old 4,382,720 budget is drift that had already landed against the old headroom since 4,295,931 was measured for #976: the first screen without this change measures 4,374,933 bytes, still inside the old budget, so this change is what crossed the line and it is 14 KiB of it. The rest is the file's stated 2% headroom, restored over the new measurement. WHY THE ROWS ARE NOT IN THESE BYTES: the preferred resolution was applied where it counts. The ranked department rows and every figure in them are built by the page script into the disclosure the region already had, so scripts/seed-first-screen.mjs seeds not one slot of them and the document grew by ids and a comment alone. What the reader must read before pressing anything — the headline sentence with its figure — is painted into the answer slot outside the `details`, so progressive disclosure is not defeated to buy the saving. Previous baseline note follows. Measured 4,195.2 KiB (4,295,931 bytes) in dist/ on 2026-08-02. RAISED ON PURPOSE for #976, and itemised rather than rolled up: this change adds EXACTLY 18,092 bytes to the first screen — 15,210 of entry graph (the two modules that compose and paint the imported headline, plus the wiring in the page entry; the checked-in fixture is a JSON import and is not counted in the graph), 1,387 of evolution.css, and 1,495 of evolution.html. The remaining 73,193 bytes of the gap to the old 4,289,536 budget is drift that had already landed against the old headroom since 4,106.1 KiB was measured for #931, and it is recorded here as its own line rather than folded into #976's number: the first screen without this change measures 4,277,839 bytes, still inside the old budget, so this change is what crossed the line and it is 18 KiB of it. The rest is the file's stated 2% headroom, restored over the new measurement. Previous baseline note follows. Measured 4,106.1 KiB in dist/ on 2026-08-02. RAISED ON PURPOSE for #931, and itemised rather than rolled up: this change adds EXACTLY 18,638 bytes to the first screen — 13,240 of entry graph (the shared chip vocabulary and the two views that paint it, split out in the entry-graph note below), 4,717 of evolution.css, and 681 of evolution.html. The remaining ~66 KiB of the gap to the old 4,092 KiB budget is drift that had already landed against the old headroom since 4,011.6 KiB was measured for #927, and it is what left this change no room; it is recorded here as its own line rather than folded into #931's number. The rest is the file's stated 2% headroom, restored over the new measurement. The bytes belong on the first screen because the import status is the first thing a reader of an unsettled export has to see: a recognition verdict that arrives after paint is one they have already acted without. Previous baseline note follows. Measured 4,011.6 KiB in dist/ on 2026-08-02. RAISED ON PURPOSE for #927: the browser-only compatibility contracts for Bedrock, Vertex AI and Azure OpenAI, and the eligibility surface on /evolution.html that is generated from them. The sum moves because two of its parts do; the split between what this change added and what had already landed against the old headroom is in each part's own note below. This records the measured first screen and restores 2% headroom. Previous baseline note follows. Measured 3,920.1 KiB in dist/ on 2026-08-01. RAISED ON PURPOSE for #901: the focused local-export activation, visible local-processing state, and responsive primary action ship on the named /evolution.html workflow; the accepted export still feeds the existing intake projection and answer. Destination modules behind import() remain excluded. This records the measured first screen and restores 2% headroom. Previous baseline: measured 3,837.2 KiB in dist/ on 2026-08-01 for #857."
    },
    {
      "id": "evolution-html",
      "label": "evolution.html (document)",
      "kind": "file",
      "path": "evolution.html",
      "budgetBytes": 310272,
      "baseline": "RAISED ON PURPOSE for #1210 by 1 KiB, being one empty paragraph inside the shareable-link control and the rationale comment above it, and nothing else. NO FIGURE, NO GRADE and no verdict is in these bytes: every word the line ever shows is written at paint time by src/finops-share-parity.js from what this browser itself retained, and the paragraph ships EMPTY because with nothing retained there is no link for a verdict to be about. It is a paragraph in the open rather than inside a disclosure element, for the reason the comment beside it gives — the test harness reads through a shut one and a real browser does not — and it is authored ABOVE the button because a lead who has already pressed has already sent. It adds no focusable node and no stylesheet rule: the line reuses .prompt-coaching-copy-status, which this block already carries. Measured 302.6 KiB in dist/ on 2026-08-06, rounded up to the next whole KiB. Previous baseline note follows. RAISED ON PURPOSE for #1206 by 3 KiB, being the shareable-link control in the answer region and the rationale comment above it: a block, a lead sentence, a button, a status paragraph and a labelled box, and NO FIGURE — the link, and every word beyond the authored lead, is written at paint time by src/finops-share-link-control.js from what this browser itself retained, so no period, no amount and no reader data is in these bytes. It ships HIDDEN and stays hidden while there is nothing of the reader's own to share, so the common first visit meets no control offering to send an invented company's spend. NEITHER STYLESHEET WAS RAISED and neither moved: the block reuses .prompt-coaching-copy, its lead, its status and its fallback, the design system secondary button, and a prompt-coaching-field textarea, so this control adds no rule to either sheet. Measured 301.4 KiB in dist/ on 2026-08-06, rounded up to the next whole KiB. Previous baseline note follows. RAISED ON PURPOSE for #1195 by 2 KiB, being the copy control in the answer region and the rationale comment above it: a block, a lead sentence, a button, a status paragraph and a labelled box, and no figure — every value in it is written at paint time. NEITHER STYLESHEET WAS RAISED and neither moved: the block reuses .prompt-coaching-copy, its lead, its status and its fallback, the design system secondary button, and a prompt-coaching-field textarea, so this control adds no rule to either sheet. Measured in dist/ on 2026-08-06, rounded up to the next whole KiB. Previous baseline note follows. RAISED ON PURPOSE for #1186 by 1 KiB, being its one part and nothing else: the grade beside the recoverable figure, the sentence slot for its explanation inside the disclosure the region already ships, and the comment stating where the rule lives. Measured 296.2 KiB in dist/ on 2026-08-06, rounded up to the next whole KiB. Previous baseline note follows. RAISED ON PURPOSE for #1185 by 8 KiB, being its one part and nothing else: the three how-we-know disclosures in the document. NEITHER STYLESHEET WAS RAISED and neither moved — the pattern reuses .figure-source, .figure-source-summary, .figure-source-state, .figure-source-shape and .figure-source-detail whole and adds no rule to either sheet, and the entry graph is unchanged. The bytes buy a CTO the finding before its qualification: the 233-character hedge between the recoverable figure and the move it implies is now 69 characters plus a one-word Illustrative marker, and the provenance, basis and limits it used to shout are one press away in one learnable disclosure, verbatim, with the internal identifiers a skeptic needs kept inside it rather than deleted. They are on the first screen because a proof a reader has to leave the answer to reach is a proof they will quote past. Previous baseline note follows. Measured 287.4 KiB in dist/ on 2026-08-05, rounded up to the next whole KiB. RAISED ON PURPOSE for #1183 by 2 KiB: the answer region — one heading, one labelled currency figure, one confidence sentence, one link — and the comment carrying the derivation of the figure, so the arithmetic travels with the number rather than living in a review thread. No new stylesheet rule and no new data source. Was: 292,864 after #1169. PRIOR NOTE: RAISED ON PURPOSE for #1169 by 3 KiB: the on-ramp section shell at the head of the import panel — an eyebrow, the question as its heading, one labelled select, the authored unchosen-state paragraph, the empty detail container, and the comment stating why the live region is a direct child of the section rather than inside a disclosure and why every control here sits below the first-run region. No option, provider name, report, console path or column is authored: all of them are painted from src/import-on-ramp.js. Previous baseline note follows. Measured 282.5 KiB (289,271 bytes) in dist/ on 2026-08-05, rounded up to the next whole KiB. RAISED ON PURPOSE for #1166 by 1 KiB: the empty recipe section in the import panel — a heading, a lead paragraph, an empty list, and the comment saying every string in it is a slot. The rows themselves are painted from src/import-recipes.js, so the document carries the container and none of the copy. Previous baseline note follows. Measured 281.3 KiB (288,048 bytes) in dist/ on 2026-08-05, rounded up to the next whole KiB. RAISED ON PURPOSE for #1141 by 1 KiB, being EXACTLY 809 bytes of src/evolution.html and nothing else: one empty div inside the routing-slate section, below the download control, plus the comment stating what paints into it, why it sits below the control, and that it holds no input of any kind. NO STYLESHEET WAS RAISED: the disclosure painted into it reuses .completeness-detail and .answer-figure-basis, and its snippet block reuses the shipped monospace preview rule with one selector appended inside evolution.css's existing headroom. The slot ships EMPTY on purpose, and this is the one case where that is not a byte-saving trick but the honest state: the snippet is a translation of the reader's own policy, so a document that authored one would be showing somebody else's rules. NO rule, model, org unit, tier, figure or period is in these bytes. Previous baseline note follows. Measured 280.5 KiB (287,239 bytes) in dist/ on 2026-08-05, rounded up to the next whole KiB. RAISED ON PURPOSE for #1138 by 1 KiB, being EXACTLY 1,296 bytes of src/evolution.html and nothing else: the routing-policy download control inside the routing-slate section — one paragraph holding one button — plus the comment stating why it sits below the painted body rather than above it, why it is authored DISABLED, and why it is in this section rather than in the page's action rail. NO STYLESHEET WAS RAISED: the button reuses .secondary-button, which this document already carries nine times, and the reason clause reuses the shipped .visually-hidden utility, so no CSS rule was added to either sheet. The control is authored rather than painted because its DISABLED state is the true state of a document whose script never ran — there is no slate, so there is no policy — and because the reason it states is the same sentence the authored status line above it already carries. NO FIGURE, no rule, no period and no reader data is in these bytes: every one of them is composed at runtime by src/routing-policy-document.js from the slate already on screen. Previous baseline note follows. Measured 279.2 KiB (285,937 bytes) in dist/ on 2026-08-05, rounded up to the next whole KiB. RAISED ON PURPOSE for #1139 by 2 KiB: the routing-score section shell — an eyebrow, the question as its heading, one authored status paragraph, and an empty body the view paints into — plus the comment stating why that status line is authored in the document rather than painted inside a disclosure. NO STYLESHEET WAS RAISED: the panel reuses .finops-panel, .answer-question, the .answer-figure trio, .action-list and .completeness-detail, and adds no CSS rule. Previous baseline note follows. Measured 284,275 bytes in dist/ on 2026-08-05, and rounded up to the next whole KiB. RAISED ON PURPOSE for #1137 by about 0.7 KiB: the routing-slate section shell — an eyebrow, the question as its heading, and an empty body the view paints — plus the comment naming what paints it and what it deliberately leaves out. Previous baseline note follows. Measured 283,260 bytes in dist/ on 2026-08-05, and rounded up to the next whole KiB. RAISED ON PURPOSE for #1113 by about 0.9 KiB: two sentences that name the scope of the two verdicts on the first screen — the spend-classification block answers how much in-scope spend the rubric scored, the circulation verdict answers whether the finding, comparison and first action are ready to send — plus the comment stating why they are authored and why they sit outside the answer block. Both are plain paragraphs behind no disclosure, because a reader who reads the two verdicts as a contradiction does so on sight and would never open a panel to resolve it. No figure, tier, band or confidence label moved, and no reader data is in these bytes. Previous baseline note follows. Measured 282,379 bytes in dist/ on 2026-08-05, and rounded up to the next whole KiB. RAISED ON PURPOSE for #1104 by about 2.8 KiB: the trust-ladder block under the intake's answer — the heading, the three rung chips, the sentence saying what this rung supports and what it does not, and the one-sentence step up — plus the comment stating why all three rungs are drawn at once and why nothing in the block is focusable. It is authored in the DECLARED state rather than left empty because that state is TRUE for a visitor who has answered nothing: the five facts on the page really are the bundled example's until they change one, so a reader whose script never ran meets a correct ladder rather than a placeholder. src/finops-trust-ladder.js repaints the same sentences, which is what proves the two copies agree. NO reader data, no declared value and no figure is in these bytes. Previous baseline note follows. Measured 279,347 bytes in dist/ on 2026-08-05, and rounded up to the next whole KiB. RAISED ON PURPOSE for #1105 by about 1.8 KiB: the roster file control beside the typed headcount field, and the two paragraphs that state — before any file is chosen — exactly which two columns are expected and that every other column is refused rather than dropped. The disclosure is authored rather than painted, and it is plain text rather than a disclosure widget, because a refusal a reader could have avoided by knowing the shape is a refusal we caused, and prose behind a control is prose most readers never open. Previous baseline note follows. Measured 277,148 bytes in dist/ on 2026-08-05, and rounded up to the next whole KiB. RAISED ON PURPOSE for #1103 by EXACTLY 4,854 bytes: the five-fact intake group in the first-run region — its legend, five labelled controls with their published option lists, the submit and clear controls, and the three-sentence answer spine — plus the note explaining why it declares no action and stores nothing. It is authored rather than built so a visitor whose script never ran still meets a labelled, keyboard-operable group and a coherent estimate beside it. Previous baseline note follows. Measured 272,294 bytes in dist/ on 2026-08-03, and rounded up to the next whole KiB. RAISED ON PURPOSE for #1102 by EXACTLY 4,498 bytes: the estimated-position block — heading, figure, detail line, and a shut disclosure holding the six working lines — plus the rationale comment above it. The figures in it are authored rather than left empty on purpose: an estimate a reader can check by hand has to survive a page whose script never ran, and the view repaints the same sentences from the module, which is what proves the two copies agree. No reader data and no declared string is in these bytes. Previous baseline note follows. Measured 267,796 bytes in dist/ on 2026-08-03, and rounded up to the next whole KiB. RAISED ON PURPOSE for #1091, by EXACTLY what that change adds and no more: 2,379 bytes for the track-record header above the answer region — a section, its question heading, the one sentence a browser keeping no period is owed, and the comment stating what is painted into the region and why this one state renders no control of its own. Measured as the difference between src/evolution.html before and after, which is the same 2,379 bytes dist/ grew by, so the build seeds none of it. NO FIGURE, NO VERDICT BADGE, NO PERIOD ROW and no disclosure is authored: every one of them is built at runtime by src/finops-track-record.js from what this browser itself holds, so no reader data and no measurement is in these bytes. What IS authored is the first-visit state itself rather than a placeholder — a browser that never runs the module reads a true sentence instead of a zero waiting to be filled in — and that is why it is markup and not a deferred fragment. PREVIOUSLY 265,417 bytes for #1092, by EXACTLY what that change adds and no more: 2,258 bytes for the track-record export/import control block beside the file picker — an eyebrow, the locality sentence a reader must be able to read without opening anything, two live regions, one button, one labelled file input, and the comment saying why the block is not inside the retention block it belongs with. Every string it will ever show beyond the authored locality sentence and the initial count is painted at runtime from src/finops-briefing-series.js, so no message, no figure and no reader data is in these bytes. They are on the first screen because a control a reader has to reveal before they can import cannot carry a record onto a new browser. PREVIOUSLY 257,164 bytes for #1025,and itemised: (a) EXACTLY 10,432 bytes, being the eight per-figure source markers on the bundled example brief plus the legend line above them and the comments stating the convention. Each marker is a native details element carrying the figure it qualifies, the state it is in, and the two disclosed terms — where the value came from and what decision it moves; (b) 4,392 bytes of drift that had already landed against the old headroom since 240,972 was measured for #1008, recorded as its own line — the document without this change measures 246,732 bytes, still inside the old 246,784 budget, so this change is what crossed the line and it is 10 KiB of it; (c) the rest is the file stated 2% headroom, restored. WHY THE WORDS ARE IN THE DOCUMENT AND NOT PAINTED: this is the one signal on the page that says a money figure was computed rather than read, and a provenance claim that arrives after paint is one the reader has already quoted the number without. The markers are keyboard-operable and state-bearing with no script at all, which is the whole reason they are native disclosures; src/finops-example-figure-sources.js repaints them from its own copy of the same sentences, so the two cannot drift. NO reader data and no imported figure is in these bytes: every value quoted is a bundled synthetic one already on the page. Previous baseline note follows. Measured 240,972 bytes in dist/ on 2026-08-02. RAISED ON PURPOSE for #1008, and itemised: (a) EXACTLY 958 bytes, being the reader-classification block on the literacy card — a container, an empty title line, an empty list the department rows are painted into, and the comment stating why the block is on the card rather than inside the disclosure beneath it; (b) 4,394 bytes of drift that had already landed against the old headroom since 235,620 was measured for #997, recorded as its own line — the document without this change measures 240,014 bytes, still inside the old 240,640 budget, so this change is what crossed the line and it is 958 bytes of it; (c) the rest is the file's stated 2% headroom, restored. The block ships EMPTY: every reader-facing label, every option, every stated assumption and every figure is painted by src/reader-classified-grade.js from src/department-workload-categories.json, so no rubric wording, NO WEIGHT, no letter and no reader's department name is in these bytes. It is markup rather than a deferred fragment because it is the control that earns the letter above it: a picker that arrives after paint is one the reader has already read a pending mark instead of. It is NOT inside the score-detail disclosure for the reason the comment beside it gives — a real browser hides a collapsed disclosure from the reader even where the test harness reads its text, and the shortfall sentence in it is the card's only instruction. Previous baseline note follows. Measured 235,620 bytes in dist/ on 2026-08-02. RAISED ON PURPOSE for #997, and itemised: (a) EXACTLY 2,060 bytes, being the two named import slots (a label with a state span and an empty unlocks line each), the literacy card's empty reason line, and the comments stating that neither is a live region and why — this page announces through one region plus the import reason beside it, and a third would make every clear speak twice; (b) 3,206 bytes of drift that had already landed against the old headroom since 230,354 was measured for #979, recorded as its own line — the document without this change measures 233,560 bytes, still inside the old 235,520 budget, so this change is what crossed the line; (c) the rest is the file's stated 2% headroom, restored. The slots ship with their state word and NOTHING else: every unlocks sentence and the whole of the withheld-grade explanation are painted by src/finops-import-slots.js. They are markup rather than a deferred fragment because a reader with JavaScript off must still meet a panel that says it reads two files, and because the state word is the accessible half of a state a stylesheet would otherwise carry alone. Previous baseline note follows. Measured 230,354 bytes in dist/ on 2026-08-02. RAISED ON PURPOSE for #979, and itemised: (a) EXACTLY 425 bytes, being four ids the imported state addresses — the example-only slots grid, recommendation and confidence blocks it withholds whole, and the heading text inside the shared evidence disclosure so the disclosure can name what it is holding — plus the comment stating why they exist; (b) 4,886 bytes of drift that had already landed against the old headroom since 225,043 was measured for #976, recorded as its own line — the document without this change measures 229,929 bytes, still inside the old 230,400 budget, so this change is what crossed the line; (c) the rest is the file's stated 2% headroom, restored. NO department row, NO figure and no fallback sentence is in these bytes: every one of them is built by src/finops-first-run-view.js from src/finops-imported-departments.js after a reader imports a file, and the build seeds none of them. Previous baseline note follows. Measured 225,043 bytes in dist/ on 2026-08-02. RAISED ON PURPOSE for #976, and itemised: (a) the imported-state headline block — an eyebrow, an empty question line, an empty description list the five slots are painted into, and the comment stating what is painted into them and why the block is five rows and never four — which is EXACTLY 1,495 bytes, measured as the difference between this document and the same document without the block; (b) 4,224 bytes of unrelated drift that had already landed against the old headroom since 219,324 was measured for #956, recorded here as its own line rather than folded into #976's number — the document without this change measures 223,548 bytes, still inside the old 224,256 budget, so this change is what crossed the line; (c) the rest is the file's stated 2% headroom, restored. The block ships EMPTY and HIDDEN: the question, the five slot labels, every figure, every fallback sentence and both provenance labels are painted by src/finops-imported-headline-view.js from src/finops-imported-headline-fixture.json, so no word a reader acts on and no reader's data is in these bytes. It is markup rather than a deferred fragment because it sits inside the first-screen answer region, whose block a deferred loader would have to fetch before the region could be painted at all. Previous baseline note follows. Measured 219,324 bytes in dist/ on 2026-08-02. RAISED ON PURPOSE for #956, and itemised: (a) the lead finding now ends on its recommended action in the SERVED bytes rather than in an anchor a script fills — scripts/seed-first-screen.mjs seeds #finops-stand-action and #finops-stand-action-basis the way it already seeds every figure above them — and that is EXACTLY 302 bytes, measured as the difference between the seeded document with and without the two edits; (b) the rationale comment stating why the recoverable figure leads and how it and the driving department are defined, which is the part of this change a later reader has to be able to check the code against, about 1.3 KiB after two trimming passes; (c) about 1.6 KiB of drift that had already landed against the old headroom since 214,087 was measured, recorded here as its own line rather than folded into this change's number; (d) the rest is the file's stated 2% headroom, restored over the new measurement. The 302 bytes belong in the initial payload for the reason the rest of the first screen is seeded at all: an action a leader is told to take first, held in an empty anchor until a module boots, is a next step the served document does not have. No figure was added, no figure was recomputed, and no node was dropped — every id in the region is the id it was. Previous baseline note follows. Measured 214,087 bytes in dist/ on 2026-08-02. RAISED ON PURPOSE, and the raise is two separate things rather than one: (a) #930 added the provider-native intake region — the question, the drop target, the file control, the example chooser, the finding's live region, the trust list, the evidence disclosure shell, and the comment stating what is painted into them — and that is EXACTLY 2,963 bytes, measured as the difference between this document and the same document without the region; (b) 1,797 bytes of unrelated drift had already landed against the old headroom since 209,327 was measured for #927, and they are recorded here as their own line rather than folded into #930's number, so the next person's baseline archaeology is not muddied. The rest of the gap to this budget is the file's stated 2% headroom, restored. The region ships with NO PROVIDER, no column name, no rate and NO FIGURE in it: every word a reader acts on is decided by src/provider-native-import.js and painted by src/provider-native-import-view.js. It is markup rather than a deferred fragment because the finding region is a live region — one that arrives after paint is one a screen reader was never told about — and because a reader with JavaScript off must see the question and the controls rather than a blank box. Previous baseline note follows. Measured 209,327 bytes in dist/ on 2026-08-02. RAISED ON PURPOSE, by 7 KiB over the previous 206,848: #927 added the hyperscaler eligibility region — the question, the disclosure shell, the two choosers, the file input, the run control, and the verdict region — and it is about 2.4 KiB of markup and comment. The other ~5 KiB of the gap landed against the old headroom since 201,875 was measured, so this number records where the document actually is rather than attributing all of it to one change. The region ships with NO PROVIDER NAMED in it: every card, field name, rejection, remedy and prohibition is painted from src/browser-compat-contracts.js, which is what makes a fourth contract appear here with no page edit — tests/browser-compat-contracts.test.js asserts that none of the three display names occurs in this markup. What does ship as markup is the verdict region's EMPTY state, and it is markup rather than a deferred fragment for two reasons: it is a live region, and a live region that arrives after paint is one a screen reader was never told about; and a reader with JavaScript off must see 'no export has been checked yet' rather than a blank box they could read as a clean bill of health. Previous baseline note follows. Measured 201,875 bytes in dist/ on 2026-08-01. RAISED ON PURPOSE, by 5 KiB over the previous 201,728: #891 added the circulation-decision region to the answer block — the eyebrow, the question, the verdict, the three ordered evidence slots, and the confidence, period and boundary lines. It ships with NO FIGURE in it: every word a reader acts on is decided by src/briefing-readiness.js and painted by src/briefing-readiness-view.js, so these bytes are the question and the slot names, not the answer. It is markup rather than a deferred fragment because it is the first-screen answer region's own verdict — a readiness claim that arrives after paint is a claim the reader had already circulated without. Previous baseline note follows. Measured 197,168 bytes in dist/ on 2026-07-31. RAISED ON PURPOSE, by 6 KiB over the previous 195,584: #821 added the three per-destination load-status regions — an eyebrow, a retry button and the comment stating what is painted into them — and they are 1,968 bytes of markup and comment. The other ~4.3 KiB of the gap landed against the old headroom since 190,818 was measured, so this number records where the document actually is rather than attributing all of it to one change. The regions ship EMPTY and HIDDEN: every word in them at runtime comes from src/finops-workspace-shell.js, so no figure and no reader's data is in these bytes. They are markup rather than a deferred fragment for the obvious reason — they are what the page shows when a deferred fragment did not arrive, so a loader cannot be the thing that fetches them. Previous baseline note follows. Measured 190,818 bytes in dist/ on 2026-07-31. RAISED ON PURPOSE, by 5 KiB over the previous 190,464: #794 added the review-and-correct disclosure to the gradeability line — the open control, the panel shell, and the comment stating what is painted into it and what may never reach it. It is about 1.9 KiB of markup and comment; the remaining ~3 KiB of the gap landed against the old headroom since 185,876 was measured, so this number records where the document actually is rather than attributing all of it to one change. Every slot in the block ships EMPTY: the queries, the classes, the matched signals, the corrected figures and the provenance count are all painted by src/query-review-view.js, so no figure and no reader's data is in these bytes. It is markup rather than a deferred fragment for the same reason the data-guarantee block below it is — the containment claim it makes has to be readable by a reviewer with JavaScript off, and the panel is a disclosure on the first-screen answer region rather than a panel of prose that could pay for its own loader. Previous baseline note follows. Measured 185,876 bytes in dist/ on 2026-07-31. RAISED ON PURPOSE, by 5 KiB over the previous 185,344: #764 added the detected-structural-columns region to the import panel — the dialect line, the six field slots, the counts, the single next action, and the always-present polite region that announces them. It is 1,781 bytes of markup and comment; the other 4,246 bytes of the gap between this measurement and the previous baseline landed against the old headroom since 181,449 was measured, so this number records where the document actually is rather than attributing all of it to one change. The region is markup rather than a deferred fragment for the same reason the data-guarantee block below it is: it is a claim about what the reader's own file does and does not contain, and it has to be readable by a reviewer with JavaScript off — the slots are empty until a file is read, so no figure ships in these bytes. Previous baseline note follows. Measured 181,449 bytes in dist/ on 2026-07-30. RAISED ON PURPOSE, by 4 KiB over the previous 181,248: #745 added the collapsed data-guarantee block to the import panel — what is processed locally, what is stored and how to clear it, what is never transmitted — which is 4,100 bytes of authored prose and markup. It belongs in the initial payload rather than behind the deferred-detail loader for the reason the deferral note below gives: one panel of prose does not pay for its own loader, and this one has to be readable and copyable by a reviewer who has JavaScript off, which is exactly the reader the claim is being defended to. It was trimmed twice before the budget was raised. Was 177,349 bytes immediately before it, after Mina's retirement pass, Iris's progressive-disclosure markup, and the comparable-peer method deferral."
    },
    {
      "id": "evolution-entry-graph",
      "label": "evolution-page.js + its static import graph",
      "kind": "module-graph",
      "entry": "/evolution-page.js",
      "budgetBytes": 4531200,
      "baseline": "RAISED ON PURPOSE for #1207 by 21 KiB, being ONE new module and the edits that make the codec consume it, and nothing else: src/finops-brief-envelope.js (the shared envelope — its versioned field list, its five named refusals with the three sentences each renders, the projection that drops unknown fields, and the deterministic serializer both transports write through) plus the static import of src/executive-finops-briefing.js it pulls in to read the figure, the destination and the grade off the SAME builder the recipient view renders from, rather than recomputing them here and being able to disagree. src/finops-shared-briefing-link.js SHRANK — it no longer defines a payload, projects a record, or holds a copy of the retained-record check — and the file-side reader, src/finops-open-shared-brief.js, is imported by /executive-briefing.html's entry and is NOT in this graph. The bytes are on the first screen because the share control that copies the link is on the first screen, and a link whose envelope is defined somewhere the copy path cannot see is exactly the drift this module exists to prevent. Measured 4,424.6 KiB in dist/ on 2026-08-06, rounded up to the next whole KiB. Previous baseline note follows. RAISED ON PURPOSE for #1210 by 13 KiB, being ONE new module and two small edits and nothing else: src/finops-share-parity.js at 10,201 bytes (the round-trip check — encode this browser's analysis with the shipped encoder, read it back through the recipient's own reader path, rebuild the executive briefing from what came out, and compare a NAMED five-field list: schema version, headline figure, destination org unit, confidence grade and reporting period), about 1 KiB of src/finops-shared-briefing-link.js (the supported-schema list and the reader that says which version a token DECLARES, so a refusal can name it) and about 1 KiB of src/finops-share-link-control.js (the paint). NO module was added for the briefing itself: the recipient side is buildExecutiveBriefing, the same function the printable briefing page calls, which src/executive-finops-briefing.js already brings into this graph — a parity check with its own copy of the grading rules would be a check that can only ever agree with itself. Measured 4,403.6 KiB in dist/ on 2026-08-06, rounded up to the next whole KiB. Previous baseline note follows. RAISED ON PURPOSE for #1206 by 34 KiB, being THREE modules and nothing else: src/finops-shared-briefing-link.js (the fragment codec — the retained-period allowlist projected into a token and back, with a schema version, a length ceiling and one named refusal per failure), src/finops-share-link-control.js (the control that mints and copies the link), and src/executive-briefing-source.js, which joins this graph for the first time because the control reads the workspace through the same total reader the executive briefing does rather than opening a second path to the store. NOTHING ELSE JOINED: the codec's serializer is src/finops-briefing-export.js's own and its validators are src/finops-workspace.js's, both already here, so the allowlist stays one place. NO STYLESHEET was raised or moved. The bytes are on the first screen because the control lives inside the answer region a lead is already reading — a share affordance a reader has to navigate to is one they will retype a figure instead of. Measured 4,390.7 KiB in dist/ on 2026-08-06, rounded up to the next whole KiB. Previous baseline note follows. RAISED ON PURPOSE for #1196 by 8 KiB, being one function, one slot and one disclosure builder inside src/finops-stand.js, which was already in this graph. NO MODULE JOINED IT: `gradedRecoverableFloor` reads the analysis envelope the region already holds — rankedDepartments[].recoverableUsd joined to literacy.departments[].gradeable — so it imports no dataset, no formatter and no helper chain, and the graph gained no edge. The bytes buy the first screen the smaller recoverable figure that survives the coverage caveat the trust panel already admits to: the modelled $51,254 is taken over all $154,500 analyzed, the graded floor is $15,925 over the $64,500 the rubric actually scored, and a finance reviewer who discounts the first for 42% coverage now has the second to hold instead of nothing. It is not deferrable: it is a headline figure beside the figure it qualifies, and a floor a reader has to unfold to reach is a floor they will conclude was hidden. NEITHER STYLESHEET WAS RAISED AND THE DOCUMENT WAS NOT EITHER — the figure reuses .stand-figure whole and its disclosure is mounted by finops-stand-view.js like the two beside it. Measured 4,356.3 KiB in dist/ on 2026-08-06, rounded up to the next whole KiB. Previous baseline note follows. RAISED ON PURPOSE for #1195 by 18 KiB, being src/finops-answer-copy.js and the shared copy helper it reuses rather than reimplements, src/coaching-summary.js, which was already in the coach page graph and is now in this one. It is not deferrable: the summary is built inside the headline paint, from that paint own headline, which is what makes the copied qualifier and the rendered one impossible to drift apart. A lazily loaded copy would be a second composition of the same five slots. Measured in dist/ on 2026-08-06, rounded up to the next whole KiB. Previous baseline note follows. RAISED ON PURPOSE for #1186 by 15 KiB, being its two parts and nothing else: src/finops-recoverable-confidence.js, the confidence rubric the answer region grades its recoverable figure with, and src/finops-recoverable-confidence-view.js, which paints the grade and its generated explanation. NEITHER STYLESHEET WAS RAISED and neither moved — the grade reuses .figure-source-state, the chip the Illustrative marker beside it already ships, and no rule was added to either sheet. The bytes buy the first screen a headline fact it did not carry: how far the figure beside it may be trusted, from named cut points a director disputing the grade can argue with, with the inputs holding it down and the evidence that would raise it generated from the same records the labelled fixture asserts on. They are on the first screen because a grade a reader has to import something to see is a grade nobody is given. Measured 4,330.2 KiB in dist/ on 2026-08-06, rounded up to the next whole KiB. Previous baseline note follows. Measured 4,315.9 KiB in dist/ on 2026-08-05, rounded up to the next whole KiB. RAISED ON PURPOSE for #1183 by 1 KiB. No module joined the graph: this is the manifest entry in src/finops/answer-spine-view.js and the two census lines in src/finops-spine.js that classify the new region. A region declared in neither file fails the build, so these bytes are the price of the region being checkable at all. Was: 4,418,560 after #1169. PRIOR NOTE: RAISED ON PURPOSE for #1169 by 15 KiB, being the whole static cost of the on-ramp: src/import-on-ramp.js (7,110 bytes), which joins the recipe list, the export package and the readiness contract into one choice per pinned adapter and checks the adapter-to-sample map at module load, and src/import-on-ramp-view.js (7,833 bytes), which paints the chooser, the live answer region and the three disclosures. No new dependency entered the graph — every contract both files read was already imported by the page — and neither module creates a Blob or opens a picker of its own: the page hands in its existing download writer and its existing file input. Previous baseline note follows. Measured 4,299.2 KiB in dist/ on 2026-08-05, rounded up to the next whole KiB. RAISED ON PURPOSE for #1168 by 1 KiB, being the whole static cost of the downgraded spend-only tier: one missing-field to supplying-recipe accessor in import-recipes.js, one outcome name, one status row, and the reset that retires the tier's figures on every other paint. The tier's own figures, its withheld table and its renderer are in spend-only-tier.js and provider-export-projection-view.js, which this page loads only after a reader selects a file, so they are NOT in this graph. Previous baseline note follows. Measured 4,298.2 KiB in dist/ on 2026-08-05, rounded up to the next whole KiB. RAISED ON PURPOSE for #1166 by 8 KiB, which is src/import-recipes.js plus the paint function that renders it. NO NEW MODULE ENTERED THE GRAPH BEHIND IT: every contract the recipes read — multi-provider-intake.js, provider-export-package.js, finops-tabular-import.js, org-query-source.js, query-sample-contract.js — was already a static import of this entry, so the growth is the new module's own bytes and nothing transitive. It is a static import rather than a deferred one because the recipe list is visible in the import panel with nothing opened, which is the whole point of the issue: a lead has to see that a spend export supplies only half the headline figure before they go and pull one. Previous baseline note follows. Measured 4,290.1 KiB in dist/ on 2026-08-05, rounded up to the next whole KiB. RAISED ON PURPOSE for #1141 by 16 KiB, which is ONE new module plus one file this graph already carried, itemised because most of it is the new one: src/gateway-shape-translation.js at 12,382 bytes (the mapping version, the policy schema and version it accepts, the two generic target shapes, the field-by-field read of the policy in one function, the untranslatable list with a reason per rule, and the stated behaviour for an unsupported version, an empty policy and a policy with no fallback route) and 3,664 bytes in src/routing-slate-view.js (the collapsed disclosure, the shape's name, the escaped snippet block and the untranslatable count beside it). Roughly half of the new module is the comment recording what neither shape carries and why it refuses rather than guesses, which is the part a platform team checking the mapping actually reads. It is NOT deferrable: the disclosure is painted by the view that paints the routing section on the first paint, so a loader would leave a reader opening an empty panel. NOTHING in it reads a clock, a network or storage — the translation is a pure function of the policy and a shape identifier, and it stamps nothing time-dependent into its output, which is what lets a test compare the translated shape exactly. NO vendor, product or proprietary configuration schema is named, imitated or referenced anywhere in it. Previous baseline note follows. Measured 4,274.4 KiB in dist/ on 2026-08-05, rounded up to the next whole KiB. RAISED ON PURPOSE for #1138 by 12 KiB, which is ONE new module plus two files this graph already carried, itemised because most of it is the new one: src/routing-policy-document.js at 7,937 bytes (the schema identifier, the version, the proposal statement the file states about itself, the per-rule guardrail and evidence blocks, and the byte-stability rules the export is worth having), 2,174 bytes in src/routing-slate-view.js (the control's enabled/disabled paint, its reason clause, and the announcement through the region the section already has) and 1,960 bytes in src/evolution-page.js (the imports, the slate held for export, the click binding and the one clock read). Roughly half of the new module is the comment recording why the file is byte-stable and what is deliberately not in it, which is the part a reviewer checking the privacy claim actually reads. It is NOT deferrable: the control is authored on the first paint and the view that paints its state is already a hard import, so a loader would leave a reader pressing an inert button. NOTHING in it reads a clock, a network or storage — the generator is a pure function of the slate and a timestamp its caller passes in. The bytes buy the thing a ranked list on a screen cannot do: carry the rules off this page as a versioned document somebody can diff, mail and argue with. Previous baseline note follows. Measured 4,262.6 KiB in dist/ on 2026-08-05, rounded up to the next whole KiB. RAISED ON PURPOSE for #1139 by 23 KiB, which is TWO files and nothing else: src/routing-rule-score.js (14,845 bytes) scores the rules the routing slate already registered against the following period, and src/routing-rule-score-view.js (7,972 bytes) paints them. Both are hard imports of the page entry because the verdict leads the panel and has to be readable without interaction; deferring them would defer the one figure the panel exists to state. Roughly half of both files is the comment recording the two thresholds and the aggregate rule, which is the part a director disputing a verdict actually reads. Previous baseline note follows. Measured 4,239.6 KiB (4,341,344 bytes) in dist/ on 2026-08-05, rounded up to the next whole KiB. RAISED ON PURPOSE for #1140 by 12 KiB, which is TWO files and nothing else: src/routing-slate.js gains the rule lifecycle (proposed / shipped / scored, each derived from evidence the envelope and the retained commitment already carry) and the unreadable-envelope branch, and src/routing-slate-view.js gains the chip vocabulary, the rank-1 lead block, the disclosure state mirroring, and the status line painter. NO NEW MODULE was added to the graph: the chips are rendered by import-status-chip.js, which was already in it. Neither file is deferrable — the section is part of the answer a first-time visitor meets with no import at all. The bytes buy the distinction the ranked list could not make: which of these dollar figures are still proposals, which have been shipped, and which have been measured. Previous baseline note follows. Measured 4,227.3 KiB (4,329,454 bytes) in dist/ on 2026-08-05, rounded up to the next whole KiB. RAISED ON PURPOSE for #1137 by about 14 KiB: src/routing-slate.js, which holds the ranking rule and its metric definitions, and src/routing-slate-view.js, which paints them. Neither is deferrable — the section is part of the answer a first-time visitor meets with no import at all, and it reads the same envelope the headline is composed from, so it cannot leave the initial payload without the answer leaving with it. Previous baseline note follows. Measured 4,213.3 KiB (4,314,432 bytes) in dist/ on 2026-08-05, rounded up to the next whole KiB. RAISED ON PURPOSE for #1126 by about 5.4 KiB, which is query-sample-contract.js joining this graph and nothing else: example-dataset.js now writes a bundled scored query sample and parses it through the same validator a reader's own sample goes through, rather than hand-building records the rubric would score without ever having been validated. That is what makes the coverage figure and the grade on the first screen the rubric's output. Previous baseline note follows. Measured 4,207.9 KiB (4,308,885 bytes) in dist/ on 2026-08-05, rounded up to the next whole KiB. RAISED ON PURPOSE for #1112 by 2,965 bytes, which is EXACTLY what this change adds to the graph and no more: no module joined it. The bytes are one placement function in src/finops-track-record.js — `leadWithWorkedDecision`, which moves the worked-decision block and this header under the region holding the page's h1 while the browser keeps no period, and moves both back the moment one is on file — plus its call in evolution-page.js and the comment stating why the blocks are MOVED rather than authored twice. Authoring a second copy toggled by `hidden` would have cost the document a full duplicate of the worked decision and read its 'bundled synthetic example' label out twice; these bytes are the cheaper half of that trade, and they buy no new state, no second storage read and no rule. Previous baseline note follows. Measured 4,204.6 KiB in dist/ on 2026-08-05, rounded up to the next whole KiB. RAISED ON PURPOSE for #1104 by about 16.6 KiB, across ONE new module and two the graph already carried: src/finops-trust-ladder.js, which is the three rungs, the sentence each one supports and withholds, the one step up from each, the rule that decides which rung the page has EARNED, and the comparison that keeps a same-rung revision silent; plus the paint calls in src/finops-declared-fact-intake-view.js and src/finops-first-run-view.js at the two moments the evidence actually changes. It is a hard import rather than a deferred one because the ladder qualifies a first-screen figure: a trust marker that arrives a request after the number is one the reader has already quoted the number without, and the rung it would replace is Declared, so a late arrival reads as an unqualified figure to anyone who does not wait. It carries no clock, no request and no storage — it is a frozen table of words plus a painter that writes through textContent, and the one piece of state it keeps is which rung each document was last TOLD about. Previous baseline note follows. Measured 4,187.8 KiB in dist/ on 2026-08-05, rounded up to the next whole KiB. RAISED ON PURPOSE for #1105 by about 17.8 KiB, across ONE new module and one existing module it pulls into the eager graph: src/hris-headcount-roster.js, which is the published roster contract restated as code, and src/delimited-text.js, the reviewed RFC4180 reader it parses through. The reader is the larger half of the bill and it is paid deliberately: a headcount import that hand-rolled its own splitter would be a second CSV parser on this page, with its own quoting and delimiter bugs, sitting next to the one that already survived review. Previous baseline note follows. Measured 4,169.3 KiB in dist/ on 2026-08-05, rounded up to the next whole KiB. RAISED ON PURPOSE for #1106 by about 13.9 KiB, and NO NEW MODULE was added — every byte is inside five modules this graph already carried, which is why it is itemised by file rather than rolled up: about 9.5 KiB in src/finops-briefing-series.js (the `basis` discriminator on an entry, the supersession pass that recomputes the link on every read, the one selection rule `realizedSeriesEntries` that everything realized filters through, the estimated-period writer, the estimate-against-bill delta and its prepared sentence, and the conditional file fields), about 2.3 KiB in src/finops-track-record.js (the realized filter at its boundary and the paragraph that renders the prepared sentence), about 1.4 KiB in src/finops-declared-fact-intake.js (the estimate as a period entry), about 0.5 KiB in src/finops-declared-fact-intake-view.js (the submit-only hook) and about 1.5 KiB in src/evolution-page.js (the imports, the one slot the intake writes through, and the clock read). None of it can be deferred: the track-record header is the first thing above the answer block, the selection rule is what that header's figure is computed from, and a delta line that arrives a request after the grade is one the reader has already forwarded the estimate without. Nothing in it carries a request: it reads and writes the one localStorage key the series already owned, and the clock is read in the page entry alone. Previous baseline note follows. Measured 4,155.4 KiB in dist/ on 2026-08-05, rounded up to the next whole KiB. RAISED ON PURPOSE for #1103 by about 17.9 KiB: src/finops-declared-fact-intake.js reads five untrusted control values into the estimators documented input shape and composes the three answer sentences, and src/finops-declared-fact-intake-view.js binds the controls and repaints the spine. Neither can be deferred: they answer the first control a visitor can touch, and a loader fetched on that touch would put a request between the question and the answer on the one path that promises neither. No coefficient or quartile boundary is duplicated in either — every figure is the estimator’s. Previous baseline note follows. Measured 4,137.5 KiB in dist/ on 2026-08-03, rounded up to the next whole KiB. RAISED ON PURPOSE for #1102 by about 36.3 KiB: src/finops-declared-fact-estimate.js (26,259 bytes — the estimator, its five named coefficients with the assumption and the dispute stated on each, and the tier ladder), src/finops-declared-fact-fixtures.js (4,331 bytes — the labelled declared-fact scenarios the region renders from and the tests pin), and about 6.6 KiB across the two modules the entry already carried, for the paint and the focus-order entry. It is a hard import rather than a deferred one because the figure is first-screen content: an estimate that arrives after a fetch is an estimate the reader has already scrolled past. Previous baseline note follows. Measured 4,101.2 KiB (4,199,624 bytes) in dist/ on 2026-08-03, and rounded up to the next whole KiB. RAISED ON PURPOSE for #1091 by 78,622 bytes, itemised file by file rather than rolled up, because one of the four numbers is most of it: src/finops-track-record.js at 16,302 bytes (the five states, the one derived action, and the painter), src/committed-saving-verdict.js at 19,554 bytes (the scorer #1090 shipped), src/commitment-verification.js at 41,578 bytes, and 1,188 bytes in src/evolution-page.js for the import, the paint helper and its one call site. THE 41,578 IS NOT A CHOICE THIS CHANGE MADE SEPARATELY: src/committed-saving-verdict.js imports src/commitment-verification.js itself, so the moment this page scores a commitment at all, both arrive. The alternative was a second scorer written here, and a page that grades the same commitment twice by two rules is the defect a byte budget cannot see. Neither module was in this graph before, because nothing on the first paint had scored a commitment before. WHY THE BYTES ARE ON THE FIRST SCREEN: the grade is the header — it is the question a returning reader opens this page with, above the snapshot that answers a different one. A verdict that arrives a request after paint is a verdict the reader has already scrolled past, and the state it would replace is the no-record sentence, so a late arrival reads as 'you have no track record' to anyone who does not wait. None of the three carries a clock, a request or storage: the scorer and the model are pure functions of the series this browser already holds and the commitment it already kept, and the painter writes through textContent only. PREVIOUSLY 4,024.4 KiB for #1092, by EXACTLY what that change adds and no more: 15.1 KiB across three modules already in this graph and NO new module — the serialize/parse/merge pair, the byte ceiling and every refusal sentence in src/finops-briefing-series.js, the two control painters in src/finops-briefing-retention-view.js, and the object URL and file read in src/evolution-page.js. It is initial payload rather than a dynamic import because the controls are authored in the document and bound at boot; deferring them would mean a reader who arrives to import a file meets an inert control until a second request lands. PREVIOUSLY 4,009.3 KiB for #1089,by EXACTLY what this change adds and no more: the headroom is left at the 4.9 KiB it already had, so the check is exactly as sensitive to the next unplanned byte as it was before. The growth is one new module, src/finops-briefing-series.js at 9,142 bytes, plus about 1.7 KiB in src/evolution-page.js (the import, the paint helper, the record-on-capture call and the load-time read) and about 0.2 KiB of replacement copy in src/finops-briefing-retention.js. The module turns the retained briefing from one overwritten payload into a series keyed by period plus provider scope, so a second import lands as a second month instead of deleting the first. WHY THE BYTES ARE ON THE FIRST SCREEN: the count and span it derives are painted above the answer, on the same paint as the restored briefing, and the migration off the single-briefing key runs on load — a reader who is told on the second paint that this browser holds three months has already read a briefing that looked like it held one. It carries no clock, no request and no reader text: it reads and writes one localStorage key, enumerates seven derived fields by hand, and every storage condition returns an empty series rather than throwing. Previous baseline note follows. Measured 3,924.3 KiB in dist/ on 2026-08-03. RAISED ON PURPOSE for #1027: the graph gains NO new module and about 22.9 KiB inside three it already carried — src/finops-briefing-export.js (the naming block the briefing file now writes, its content filter, and the integer schema version a reader branches on), src/finops-briefing-restore.js (the version branch, the sanitizer that reads a naming block back out of an untrusted file, and the stated note a pre-#1027 file wears) and src/local-import-flow.js (the painter for the reopened brief's names, its provenance and that note). All three are already static imports of this entry, because the export button and the reopen control are on the first paint. None of the new code carries a clock, a request or storage: the writer is pure, the reader is total and parses text the reader themselves chose, and the painter writes through textContent only. The remainder of the gap to the old 4,008,960 budget is drift that had already landed against the old headroom since 3,929,600 bytes was measured for #1020. Previous baseline note follows. Measured 3,837.5 KiB (3,929,600 bytes) in dist/ on 2026-08-03. RAISED ON PURPOSE for #1020: the graph gains EXACTLY 16,128 bytes of new modules — src/finops-bundled-next-step.js at 11,864 bytes (the derivation rule, its named data states, and the circulation-action adapter) and src/finops-bundled-next-step-view.js at 4,264 bytes (the painter for the where-do-I-start layer) — plus 1,480 bytes in src/evolution-page.js for the imports and the two call sites. Both are static imports rather than deferred ones because they compose the step and the checkpoint painted in the first synchronous pass, before any fetch resolves. Neither carries a clock, a request, or any reader data: the derivation is a pure function of a static dataset, and the JSON dataset it reads is not counted in this graph. The remainder of the gap to the old 3,907,584 budget is drift that had already landed against the old headroom. Previous baseline note follows. Measured 3,740.6 KiB (3,830,397 bytes) in dist/ on 2026-08-02. RAISED ON PURPOSE for #997, and the raise is two separate things rather than one: (a) #997 added ONE module to this graph, src/finops-import-slots.js at 7,085 bytes — the two-slot model, its wording, and the painter both surfaces read from — and grew src/evolution-page.js by 3,527 bytes (the one sync that paints slots and card together, plus the suppression decision now held for the retention capture) and src/finops-briefing-retention.js by 1,576 (the attribution slot the payload carries and the restore that reads it), which is EXACTLY 12,188 bytes measured file by file; (b) 72,452 bytes of unrelated drift had already landed against the old headroom since 3,745,757 was measured for #979, recorded here as its own line — the graph without (a) measures 3,818,209 bytes, still inside the old 3,821,568 budget, so this change is what crossed the line and it is 12 KiB of it. The new module is a static import rather than a deferred one because it decides what the literacy card is allowed to claim on the SAME paint as the import result: a card that shows a dash and gains its reason a request later is a card the reader has already read as a broken grade. It carries no clock, no request and no storage — it is a frozen table of wording plus two painters that write text and aria state into nodes the document already ships. Previous baseline note follows. Measured 3,658.0 KiB (3,745,757 bytes) in dist/ on 2026-08-02. RAISED ON PURPOSE for #979, and the raise is two separate things rather than one: (a) #979 added ONE module to this graph, src/finops-imported-departments.js at 8,123 bytes — the composer that ranks the reader's own groups from the imported envelope and words the fallback — and grew src/finops-first-run-view.js by 4,912 bytes (the imported state of the region: withhold the synthetic slots, repaint the headline, refill the shared disclosure, restore on clear), src/finops-first-run.js by 638 and src/evolution-page.js by 593 for the ids and the one call site, which is EXACTLY 14,266 bytes measured file by file; (b) 68,077 bytes of unrelated drift had already landed against the old headroom since 3,663,414 was measured for #976, recorded here as its own line — the graph without (a) measures 3,731,491 bytes, still inside the old 3,737,600 budget, so this change is what crossed the line and it is 14 KiB of it. The composer is a static import rather than a deferred one for the reason #976 recorded for the two modules beside it: it paints inside the first-screen answer region on the SAME paint as the import result, and a drill-down that arrives a request later leaves the reader looking at a region that still says 'Bundled synthetic example' after their own file landed — which is exactly the two-summaries state this change exists to remove. It carries no clock, no request and no storage: it ranks groups the analysis already published and returns a plain object. Previous baseline note follows. Measured 3,577.6 KiB (3,663,414 bytes) in dist/ on 2026-08-02. RAISED ON PURPOSE for #976, and the raise is two separate things rather than one: (a) #976 added TWO modules to this graph — src/finops-imported-headline.js at 9,485 bytes, which composes the five imported-state slots from the checked-in contract, and src/finops-imported-headline-view.js at 4,813 bytes, which paints them — and grew src/evolution-page.js by 912 bytes for the import and the two call sites, which is EXACTLY 15,210 bytes measured file by file. The contract itself, src/finops-imported-headline-fixture.json at 6,935 bytes, is a JSON import and is NOT counted in this graph. (b) 61,746 bytes of unrelated drift had already landed against the old headroom since 3,502.4 KiB was measured for #931, and it is recorded here as its own line rather than attributed to this change. The graph without (a) measures 3,648,204 bytes, still inside the old 3,658,752 budget — so this change is what crossed the line, and it is 15 KiB of it. Both modules are static imports rather than deferred ones because they paint inside the first-screen answer region on the same paint as the import result: a headline slot that arrives after the figure beside it is a slot the reader has already read the answer without. Neither carries a clock, a request, or any reader data — the composer reads one analysis envelope and the published peer-cohort fixture and returns a plain object. Previous baseline note follows. Measured 3,502.4 KiB in dist/ on 2026-08-02. RAISED ON PURPOSE for #931, and the raise is two separate things rather than one: (a) #931 added ONE module to this graph, src/import-status-chip.js at 7,623 bytes — the chip vocabulary the recognition surface and the provider-native intake now share — and grew the two views that paint it by 3,282 and 2,335 bytes, which is EXACTLY 13,240 bytes measured file by file; (b) about 66 KiB of unrelated drift had already landed against the old headroom since 3,422.8 KiB was measured for #927, and it is recorded here as its own line rather than attributed to this change. The graph without (a) measures 3,489.5 KiB, still inside the old 3,492 KiB budget — so this change is what crossed the line, and it is 13 KiB of it. The new module is a static import rather than a deferred one because it decides the state of the FIRST thing on the surface: which provider, how confident, and what is uncertain. A chip that arrives after paint is a state the reader already read the figure without. It carries no clock, no I/O and no reader data — it is a frozen table of words, marks and tones plus two mappers. Previous baseline note follows. Measured 3,422.8 KiB in dist/ on 2026-08-02. RAISED ON PURPOSE, by 87 KiB over the previous 3,405 KiB: #927 added four modules to this graph — the contract manifest, the deterministic fixtures, the pure evaluator, and the view that paints the eligibility surface — totalling 49.1 KiB. The remaining ~34 KiB of the gap landed against the old headroom since 3,339.2 KiB was measured, so this number records where the graph actually is rather than attributing all of it to one change. The four are static imports rather than a deferred module because the surface answers 'will my Bedrock, Vertex AI or Azure OpenAI export work here?' BEFORE a file is chosen: the two choosers have to be populated for the control to be usable at all, and a compatibility promise that arrives after the reader has already tried is not a promise. The evaluator carries no clock and no I/O, and the fixtures are static data with hardcoded dates, so none of these bytes is a figure or a reader's data. Previous baseline note follows. Measured 3,339.2 KiB in dist/ on 2026-08-01. RAISED ON PURPOSE for #901: the small activation state module is statically wired to the live import entry so chooser, validation, accepted projection, no-reload reset, and the settle that hands the disabled action back cannot drift into separate flows. This records the measured graph and restores 2% headroom. Previous baseline: measured 3,271.0 KiB in dist/ on 2026-08-01 for #857."
    },
    {
      "id": "footer-entry-graph",
      "label": "site-footer-page.js + its static import graph",
      "kind": "module-graph",
      "entry": "/site-footer-page.js",
      "budgetBytes": 38912,
      "baseline": "Measured 37,947 bytes in dist/ on 2026-08-02. RAISED ON PURPOSE, by 1 KiB over the previous 37,888: #946 made the About Shiplog band name every destination the site navigation offers — it named four of the eight — and gave each one the sentence the home page's 'Where everything is' list already uses for it. The bytes are copy and nothing else: the DEMOS table grew EXACTLY 676 bytes (the four rows Social, People, Paint and Agent observatory, plus the four descriptions that were reworded to match the home page), and 102 bytes of comment came back out, so src/site-footer.js grew 574 bytes and no other file in the graph moved. No module was added. They belong in the initial payload because they are the band's text on all sixteen pages: src/site-footer-page.js imports src/site-footer.js for initSiteFooter(), and this is the only directory of the site on the pages whose body carries none. Previous baseline note follows. Measured 36,470 bytes in dist/ on 2026-07-31. RAISED ON PURPOSE, by 8 KiB over the previous 29,696: #783 gave the follow-up form a success state, and src/follow-up-confirmation.js (5,964 bytes) is a hard import of src/site-footer.js, so it rides in this graph on every page. It is behaviour, not copy: the receipt that replaces the form once a request lands, the disabled submit and hidden form that make a second send unreachable, and the reopen control that puts the form back. It cannot be deferred behind the disclosure — the panel it belongs to is the one this entry exists to wire — and it is one file rather than two so the footer's panel and the FinOps/briefing panel (src/finops-contact.js imports the same module) cannot word a landed request differently. Its comments were cut back before the budget was raised; stripping the rest would leave the graph over the old number anyway. Previous baseline note follows. Measured 29,083 bytes in dist/ on 2026-07-31. The second module script /evolution.html loads, tracked because it is part of the same initial payload and grows independently of the page entry. RAISED ON PURPOSE, by 2 KiB over the previous 27,648: #757 rewrote the About Shiplog band so it opens with what a visitor can do here and names the four demos with the question each one answers, and src/site-footer.js is the single source of truth those strings live in — src/site-footer-page.js imports it for initSiteFooter(), so the copy rides in this graph whether or not the disclosure is ever opened. The bytes are the copy and its rationale, not a new module: the comments were cut back twice before the budget was raised, and stripping every remaining one still leaves the graph over the old number. Splitting the strings into a module the entry does not import would take them out of this payload, but it would also end the guarantee that the markup on all sixteen pages and the copy the tests pin come from one file, which is the property that keeps the band identical everywhere."
    },
    {
      "id": "evolution-css",
      "label": "evolution.css",
      "kind": "file",
      "path": "evolution.css",
      "budgetBytes": 334848,
      "baseline": "Measured 328,224 bytes in dist/ on 2026-08-03. RAISED ON PURPOSE for #1025: EXACTLY 2,512 bytes, being the figure-source marker rules and the comment stating the convention they follow. NO NEW TOKEN is declared — the chip reuses .brief-provenance and its shipped silhouettes, the focus ring is the page own --focus-ring treatment, and every colour in the block is an existing custom property. The rest is the file stated 2% headroom, restored. Previous baseline note follows. Measured 318,875 bytes in dist/ on 2026-08-02. RAISED ON PURPOSE for #997, and itemised: (a) the two import slots' rules — the wrapping grid, the present and missing treatments on each card's left rule, the state word, the unlocks line, and the literacy card's reason line — which is EXACTLY 1,365 bytes; (b) 6,039 bytes of unrelated drift that had already landed against the old headroom since 311,471 was measured for #976, recorded as its own line — the sheet without this change measures 317,510 bytes, still inside the old 318,464 budget, so this change is what crossed the line; (c) the rest is the file's stated 2% headroom, restored. These rules are render-blocking because the import panel is a first-screen region. Present and missing are marked by the state word in the markup AND by aria-disabled; the solid-versus-dashed rule here is a fourth channel on top of those and never the only one, so greyscale and a sheet that failed to load both keep the state. Previous baseline note follows. Measured 311,471 bytes in dist/ on 2026-08-02. RAISED ON PURPOSE for #976, and itemised: (a) the imported-state headline's rules — the block, its question line, the slot list, and the supported/fallen-back treatment on each row — which is EXACTLY 1,387 bytes; (b) 6,134 bytes of unrelated drift that had already landed against the old headroom since 303,950 was measured for #930, recorded as its own line — the sheet without this change measures 310,084 bytes, still inside the old 310,272 budget, so this change is what crossed the line; (c) the rest is the file's stated 2% headroom, restored. These rules are render-blocking because the block is part of the first-screen answer region. A fallen-back slot is marked by a dashed rule AND by its own provenance label in words, never by colour alone. Previous baseline note follows. Measured 303,950 bytes in dist/ on 2026-08-02. RAISED ON PURPOSE, and the raise is deliberately itemised because it is bigger than this change: (a) #930 added the provider-native intake's rules — the drop target and its dragging state, the finding's three slots and the four state rules on its left rule, the trust list, the evidence disclosure, and the one-column treatment under 640px — and that is EXACTLY 3,647 bytes, measured as the difference between this sheet and the same sheet without the block; (b) 5,289 bytes of unrelated drift had already landed against the old headroom since 295,014 bytes (288.1 KiB) was measured for #901, and that drift is what left this change no room, so it is recorded here as its own labelled line and NOT rolled into #930's number; (c) the remainder is the file's stated 2% headroom, restored over the new measurement. These rules are render-blocking because the intake is a first-screen region: an answer block whose drop target and state treatments arrive late is one a reader has already mis-read. State is in data attributes and in the words of the finding itself, never in colour alone. Previous baseline note follows. Measured 288.1 KiB in dist/ on 2026-08-01. RAISED ON PURPOSE for #901: the primary local-export action has explicit idle, reading, ready, and error treatments plus a one-column 640px layout; state words remain in markup, not colour alone. These rules are render-blocking because the activation is the import panel's primary action. This records the measured sheet and restores 2% headroom. Previous baseline: measured 289,101 bytes in dist/ on 2026-08-01 for #891."
    },
    {
      "id": "styles-css",
      "label": "styles.css",
      "kind": "file",
      "path": "styles.css",
      "budgetBytes": 92160,
      "baseline": "Measured 89,498 bytes in dist/ on 2026-08-02. RAISED ON PURPOSE, by 3 KiB over the previous 89,088: #943 added the Social composer's image-description refusal — the banded, glyph-led error line beside the field — and the read-path note that names an undescribed stored image on the feed and on a People profile; about 0.4 KiB of rules and comment. The rest of the gap landed against the old headroom since 87,314 was measured. The rules ship in this sheet because both surfaces do: social.html and profile.html link no other stylesheet, and an unstyled refusal is a sentence with nothing marking it as one. Previous baseline note follows. Measured 87,314 bytes in dist/ on 2026-07-31. RAISED ON PURPOSE, by 3 KiB over the previous 86,016: #783 added the footer receipt's rules — the card that stands where the form was, its check mark, the address that wraps at any width, and the reopen control at narrow widths — about 1.1 KiB of rules and comment; the rest of the gap landed against the old headroom since 83,838 was measured. The rules ship in this sheet because the panel does: the band is on every page of the site, and a receipt with no styling is a bare paragraph in the middle of a panel. Previous baseline note follows. Measured 83,838 bytes in dist/ on 2026-07-30. Shared across the site and render-blocking here; tracked so growth landed for another page still shows up against this one's budget."
    },
    {
      "id": "journey-consolidated-css",
      "label": "finops-journey-consolidated.css",
      "kind": "file",
      "path": "finops-journey-consolidated.css",
      "budgetBytes": 6144,
      "baseline": "Measured 5,695 bytes in dist/ on 2026-07-30. The third render-blocking stylesheet /evolution.html links, shared with the Savings Action Center."
    }
  ]
}
