{
  "$comment": "Size budget for what /evolution.html sends a reader before they interact with anything. Enforced by scripts/check-size-budget.mjs against dist/, wired into `npm run verify:build` and therefore into `npm run check`. Every budgetBytes below is a MEASUREMENT of the post-collapse page as built on 2026-07-30 from this branch, plus the stated headroom — not a target anyone hopes to reach.",
  "headroom": "2% over the measured baseline, rounded up to the next whole KiB",
  "$headroomRationale": "Small on purpose. The value of this check is that it notices an unplanned addition, so wide headroom would let a module-sized regression land silently and no headroom would fail on a comment. 2% is roughly a paragraph of prose on the document and roughly one small module on the entry graph, which is the granularity worth arguing about in review.",
  "$deferralNote": "Honest accounting of the deferral that landed with this file, because the numbers did not come out the way the change was scoped to. Moving the comparable-peer method prose out of evolution.html took 2,343 bytes out of the document; the fallback paragraph and the comment explaining it put 2,125 back, so the document is 177,349 bytes against 177,567 before — a net 218 bytes. The loader that makes the deferral work, src/finops-deferred-detail.js, is a hard import of the page entry, so the entry graph is about 8 KiB LARGER than before. The initial payload therefore grew, not shrank. The reason is that this page has no evidence MODULE that is disclosure-only: every figure on it, including the seven headline disclosures, is composed eagerly by modules the headline itself depends on (ranking-reproducibility.js, for instance, also supplies the headline's laggard name and provenance), so none of them can leave the initial payload without moving a figure the answer rests on. What is deferrable here is authored prose, and one panel of prose does not pay for its own loader. The mechanism is in place and the marginal cost of the next panel is only the prose it removes; these baselines record where the page actually is today rather than where a second panel would put it.",
  "artifacts": [
    {
      "id": "evolution-html",
      "label": "evolution.html (document)",
      "kind": "file",
      "path": "evolution.html",
      "budgetBytes": 190464,
      "baseline": "Measured 185,876 bytes in dist/ on 2026-07-31. RAISED ON PURPOSE, by 5 KiB over the previous 185,344: #764 added the detected-structural-columns region to the import panel — the dialect line, the six field slots, the counts, the single next action, and the always-present polite region that announces them. It is 1,781 bytes of markup and comment; the other 4,246 bytes of the gap between this measurement and the previous baseline landed against the old headroom since 181,449 was measured, so this number records where the document actually is rather than attributing all of it to one change. The region is markup rather than a deferred fragment for the same reason the data-guarantee block below it is: it is a claim about what the reader's own file does and does not contain, and it has to be readable by a reviewer with JavaScript off — the slots are empty until a file is read, so no figure ships in these bytes. Previous baseline note follows. Measured 181,449 bytes in dist/ on 2026-07-30. RAISED ON PURPOSE, by 4 KiB over the previous 181,248: #745 added the collapsed data-guarantee block to the import panel — what is processed locally, what is stored and how to clear it, what is never transmitted — which is 4,100 bytes of authored prose and markup. It belongs in the initial payload rather than behind the deferred-detail loader for the reason the deferral note below gives: one panel of prose does not pay for its own loader, and this one has to be readable and copyable by a reviewer who has JavaScript off, which is exactly the reader the claim is being defended to. It was trimmed twice before the budget was raised. Was 177,349 bytes immediately before it, after Mina's retirement pass, Iris's progressive-disclosure markup, and the comparable-peer method deferral."
    },
    {
      "id": "evolution-entry-graph",
      "label": "evolution-page.js + its static import graph",
      "kind": "module-graph",
      "entry": "/evolution-page.js",
      "budgetBytes": 3186688,
      "baseline": "Measured 3,050.1 KiB in dist/ on 2026-07-31. RAISED ON PURPOSE, by 70.5 KiB over the previous 3,114,496: #793 added the human-correction seam behind the imported grade. Of that, about 16 KiB is this change — src/query-label-overrides.js is 11.5 KiB (the seeded stratified sampler, the three stratum weights with the assumption each encodes, and the override model that re-grades a corpus through gradeImportedCorpus and recoverableSpendUsd rather than reimplementing either), and about 4.5 KiB is the evidence record src/query-classification.js now publishes beside every class: the token that fired, the rule id, and the containment argument that lets a token be published at all. The remaining ~55 KiB landed against the old headroom since 2,981.4 KiB was measured. Neither file can leave the initial payload: evolution-page.js routes the imported corpus's grade, coverage and recoverable figure through applyOverrides on every recomputation, including the first paint of an imported answer, so deferring it would defer the headline number itself. The new module's comments were cut back by 1 KiB before this budget was raised. Previous baseline note follows. Measured 2,981.4 KiB in dist/ on 2026-07-31. RAISED ON PURPOSE, by 69 KiB over the previous 3,043,328: #767 added the gradability gate the answer region reads before it quotes a figure out of a reader's own export. Of that, src/export-gradability.js and src/query-gradeability-reasons.js are about 10 KiB — the four states, the bar derived from the coverage tiers, and the not-gradeable reason copy split out of query-literacy.js so a surface can render a reason without importing the rubric, the classifier and the sampler. The remainder landed against the old headroom since 2,913.7 KiB was measured. The gate cannot leave the initial payload: it decides whether the headline's figures may be on screen at all, and finops-stand-view.js reads it on the first paint, so deferring it would defer the answer. Previous baseline note follows. Measured 2,913.7 KiB in dist/ on 2026-07-31. RAISED ON PURPOSE, by 77 KiB over the previous 2,964,480: #765 added the structural signal families to the query classifier, which this page already carries. The growth is 39 KiB in three parts — src/query-classification.js grew by 12 KiB for the four structural detectors, src/query-signal-families.js is 15 KiB of weight constants and the assumption each one encodes, and src/prompt-prose-segmentation.js is 11 KiB reached now by the classifier. The segmenter is the one worth arguing about, because the classifier could have counted code fences and labelled lines itself in a fraction of the bytes. It does not, deliberately: the prose classifier already defines what a code block and a labelled line are, and a second definition would let the same thread be called structured on one surface and unstructured on another with no way to tell which was right. None of the three can leave the initial payload — the reproducibility disclosure on the answer region names the family and weight behind every class, and it is composed eagerly with the rest of the headline. What DID stay out is the classifier itself: the disclosure imports only src/query-signal-families.js, which holds integers and authored English and imports nothing, so the rubric JSON and the tier table are not in this number on account of the disclosure. Previous baseline note follows. Measured 2,905,706 bytes in dist/ on 2026-07-30, after the answer spine landed — the page entry and everything it reaches by static import, which is what a browser must fetch and parse before this page is interactive. A module moved behind a dynamic import() leaves this number; a fetched JSON fragment was never in it. RAISED ON PURPOSE, by 58 KiB over the previous 2,905,088: src/finops-spine.js is 26,423 bytes in dist/ and the rest is growth that landed against the old headroom since the previous baseline of 2,847,199 was measured. The spine belongs in the initial payload rather than behind a dynamic import because the page reads its region classification and its state before the first paint — src/finops-stand-view.js and the page entry both call applyFinopsSpine() on the path that renders the answer region — so deferring it would defer the answer."
    },
    {
      "id": "footer-entry-graph",
      "label": "site-footer-page.js + its static import graph",
      "kind": "module-graph",
      "entry": "/site-footer-page.js",
      "budgetBytes": 37888,
      "baseline": "Measured 36,470 bytes in dist/ on 2026-07-31. RAISED ON PURPOSE, by 8 KiB over the previous 29,696: #783 gave the follow-up form a success state, and src/follow-up-confirmation.js (5,964 bytes) is a hard import of src/site-footer.js, so it rides in this graph on every page. It is behaviour, not copy: the receipt that replaces the form once a request lands, the disabled submit and hidden form that make a second send unreachable, and the reopen control that puts the form back. It cannot be deferred behind the disclosure — the panel it belongs to is the one this entry exists to wire — and it is one file rather than two so the footer's panel and the FinOps/briefing panel (src/finops-contact.js imports the same module) cannot word a landed request differently. Its comments were cut back before the budget was raised; stripping the rest would leave the graph over the old number anyway. Previous baseline note follows. Measured 29,083 bytes in dist/ on 2026-07-31. The second module script /evolution.html loads, tracked because it is part of the same initial payload and grows independently of the page entry. RAISED ON PURPOSE, by 2 KiB over the previous 27,648: #757 rewrote the About Shiplog band so it opens with what a visitor can do here and names the four demos with the question each one answers, and src/site-footer.js is the single source of truth those strings live in — src/site-footer-page.js imports it for initSiteFooter(), so the copy rides in this graph whether or not the disclosure is ever opened. The bytes are the copy and its rationale, not a new module: the comments were cut back twice before the budget was raised, and stripping every remaining one still leaves the graph over the old number. Splitting the strings into a module the entry does not import would take them out of this payload, but it would also end the guarantee that the markup on all sixteen pages and the copy the tests pin come from one file, which is the property that keeps the band identical everywhere."
    },
    {
      "id": "evolution-css",
      "label": "evolution.css",
      "kind": "file",
      "path": "evolution.css",
      "budgetBytes": 278528,
      "baseline": "Measured 266.4 KiB (272,819 bytes) in dist/ on 2026-07-31. RAISED ON PURPOSE, by 7 KiB over the previous 271,360: #795 made the residue-correction list keyboard- and screen-reader-operable, and 1,607 bytes of that is rules and comment in this sheet — the focus ring the roving list row now needs (it is the tab stop, so it is the thing that must show a ring, and the ring is drawn inset because an outward one overlaps its neighbour on a 10px gap), the three-state chip that replaced a CSS ::before glyph (a filled wash per the chip rule, an edge moved off --import-line because that token is 2.11:1 on #fff and under the 3:1 a non-text state indicator owes at 12px), and the visible 'Item N of M' line. The other ~5.5 KiB of the gap landed against the old headroom since 259.3 KiB was measured. None of it can be deferred: this sheet is render-blocking on the page and a focus ring that arrives after a stylesheet fetch is a focus ring a keyboard user has already moved past. The comments on the new rules were cut back once before this budget was raised. Previous baseline note follows. Measured 259.3 KiB in dist/ on 2026-07-31. RAISED ON PURPOSE, by 6 KiB over the previous 265,216: #767 added two rules for the gradability line on the answer region — the grouping and the warn colour its suppressed state turns on. Its three slots reuse the label and basis type already on this region rather than declaring their own, so the new bytes are the two rules and the comment above them; the rest of the gap landed against the old headroom. Previous baseline note follows. Measured 259,456 bytes in dist/ on 2026-07-30. Render-blocking on this page, so it is initial payload in the strictest sense."
    },
    {
      "id": "styles-css",
      "label": "styles.css",
      "kind": "file",
      "path": "styles.css",
      "budgetBytes": 89088,
      "baseline": "Measured 87,314 bytes in dist/ on 2026-07-31. RAISED ON PURPOSE, by 3 KiB over the previous 86,016: #783 added the footer receipt's rules — the card that stands where the form was, its check mark, the address that wraps at any width, and the reopen control at narrow widths — about 1.1 KiB of rules and comment; the rest of the gap landed against the old headroom since 83,838 was measured. The rules ship in this sheet because the panel does: the band is on every page of the site, and a receipt with no styling is a bare paragraph in the middle of a panel. Previous baseline note follows. Measured 83,838 bytes in dist/ on 2026-07-30. Shared across the site and render-blocking here; tracked so growth landed for another page still shows up against this one's budget."
    },
    {
      "id": "journey-consolidated-css",
      "label": "finops-journey-consolidated.css",
      "kind": "file",
      "path": "finops-journey-consolidated.css",
      "budgetBytes": 6144,
      "baseline": "Measured 5,695 bytes in dist/ on 2026-07-30. The third render-blocking stylesheet /evolution.html links, shared with the Savings Action Center."
    }
  ]
}
