{
  "name": "wawalu-agent-lab",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "node scripts/build.mjs",
    "benchmark:paint": "node scripts/benchmark-paint.mjs",
    "verify:build": "node scripts/verify-build.mjs dist",
    "test": "node --test",
    "check": "npm test && npm run build && npm run verify:build"
  },
  "devDependencies": {
    "wrangler": "4.110.0"
  }
}
