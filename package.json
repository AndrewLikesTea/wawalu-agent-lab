{
  "name": "wawalu-agent-lab",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "node scripts/build.mjs",
    "test": "node --test",
    "check": "npm test && npm run build"
  },
  "devDependencies": {
    "wrangler": "4.110.0"
  }
}

