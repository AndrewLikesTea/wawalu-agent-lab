{
  "note": "Moved to seed-records.js. The representative decision and release records are module constants now so a cold visitor's first render already contains them instead of waiting on this request. Nothing reads this file; the empty arrays only keep a stale client degrading to no examples rather than to an error.",
  "decisions": [],
  "releases": []
}
